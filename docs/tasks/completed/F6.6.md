# F6.6 — Recovery e testes de falha

> **Gate:** `COMPLETED_LOCAL`
> **Lifecycle:** `PROMOTION_PENDING`
> **Branch:** `task/f6.6-recovery-failures`
> **Baseline Git:** `638681638a341df9046f784b79140f4e40124032` (`main == origin/main` antes da branch)
> **Checkpoint READY:** `checkpoint/f6.6-ready` → `2d01cca`, somente local
> **Commit de produto:** `1d5467457cf99c4ee34d69000630de1b1aa0900b`
> **Checkpoint completo:** `checkpoint/f6.6-complete`, somente local, apontando para o commit de certificação
> **Executor:** `Codex`, único escritor autorizado
> **Autorizado em:** `2026-08-16T13:02:13-03:00`
> **Runtime:** `C:\Users\walla\OneDrive\Desktop\ai-engineering-harness\.venv\Scripts\python.exe` — Python 3.12.13

## Dependência terminal comprovada

A F6.6 começou somente depois do encerramento terminal da F6.5. O produto foi incorporado pelo PR
[#77](https://github.com/Wf-ops1/xXHarnessinfraXx/pull/77) no merge `c049125`, cuja CI de `push`
`31953772121` passou 10/10 jobs da matriz e o gate `CI required`.

A reconciliação administrativa [#78](https://github.com/Wf-ops1/xXHarnessinfraXx/pull/78) encerrou
no head final `fcc927e9f70e2a00fe1b9973512401d22ad470a2`, certificado pelo run `31955779575`, e
foi incorporada pelo merge commit `638681638a341df9046f784b79140f4e40124032`. A CI pós-merge
[#31956649961](https://github.com/Wf-ops1/xXHarnessinfraXx/actions/runs/31956649961), evento `push`,
terminou `success` nesse SHA exato com 10/10 jobs da matriz e `CI required`. Nenhuma branch ou tag foi
removida.

## Problema comprovado

O plano exige fault injection em nove fronteiras e, para cada uma, estado persistido, operação
idempotente, comportamento de `resume`, cleanup permitido e evidência produzida. A auditoria do
baseline encontrou recovery distribuído em testes antigos, sem uma matriz operacional única, e duas
lacunas explícitas:

1. `ExternalWorktreeManager.create_worktree()` publica a referência `CREATING` em
   `src/ai_engineering_harness/workspace/git_worktree.py:331`, executa `git worktree add` na linha 335
   e só publica `ACTIVE` na linha 358. Não existe caminho de recovery entre esses pontos. O probe
   controlado interrompeu a segunda publicação e a repetição observou:

   ```text
   reference=CREATING path_exists=True retry=WorktreeCollisionError
   ```

   Portanto, o efeito Git existe, mas a operação não é idempotente e não consegue reconciliar sua
   própria referência durável.

2. `KnowledgeTransactionManager.recover_if_needed()` em
   `src/ai_engineering_harness/knowledge/transaction.py:85-104` transforma o último `PREPARED` em
   `COMMITTED` apenas acrescentando uma linha. O probe criou somente o journal, sem staging e sem
   `current.json`, e observou:

   ```text
   outcome=RECOVERED_tx-missing current_exists=False staging_exists=False
   ```

   Essa evidência negativa pertence ao reparo F6.7. A F6.6 deve congelá-la e documentá-la; não pode
   alterar a transação knowledge nem apresentar esse caminho como recuperado.

As sete fronteiras restantes e as partes já seguras de model/tool/promotion possuem fault injection
real por storage, CAS, journal e managers controlados. A baseline selecionada executou os testes de
worktree, contexto, model outcome, tool intent/outcome, journal, aprovação, candidate, promoção e
knowledge e passou:

```text
13 passed, 183 deselected in 43.84s
```

## Baseline conhecido

- `.git` existe; a branch exclusiva nasceu limpa de
  `main == origin/main == 638681638a341df9046f784b79140f4e40124032` e não possui upstream.
- `git status --short --branch` retornou somente `## task/f6.6-recovery-failures`.
- A CI autoritativa da base é `31956649961`, `push`, `completed/success` no mesmo SHA.
- Runtime congelado: `.venv\Scripts\python.exe`, Python 3.12.13.
- Nenhum dossiê ativo existia antes desta tarefa; mudanças e refs das tarefas anteriores foram
  preservadas.
- A baseline de recovery selecionada passou `13/13`; os dois probes negativos acima foram executados
  em repositórios/diretórios temporários fora do checkout e não deixaram produto alterado.

## Contrato técnico congelado

1. Retry de `create_worktree(execution_id)` é idempotente somente para a mesma identidade durável:
   project root, project ID, execution ID, base SHA, branch original, path externo e branch do
   worktree devem coincidir exatamente.
2. Referência `CREATING` sem efeito Git pode continuar a criação. Referência `CREATING` com branch e
   path válidos deve revalidar o worktree e publicar `ACTIVE`, sem executar um segundo `git worktree
   add`. Metade inconsistente do efeito falha fechada e preserva a evidência.
3. Referência `ACTIVE` válida retorna o mesmo worktree após revalidação. `FAILED`,
   `CLEANUP_PENDING`, `REMOVED`, base avançada, path/branch ausente ou identidade divergente não são
   convertidos em sucesso.
4. Nenhum recovery usa `--force`, remove branch, apaga path, altera o checkout original ou aceita
   worktree sujo/divergente.
5. Contexto recupera somente decisão/payload durável; model response sem outcome durável e tool pair
   parcial exigem intervenção, sem repetir efeito ambíguo.
6. Candidate, aprovação e promoção continuam usando os eventos write-ahead/CAS e os managers
   canônicos; recovery nunca cria segundo commit, segunda decisão ou segundo cherry-pick.
7. Append falho preserva o journal canônico; somente um temporário gerenciado, íntegro e não ambíguo
   pode ser promovido pela autoridade de storage existente.
8. A matriz pública documenta, para os nove pontos, estado, idempotência, `resume`, cleanup e
   evidência. Ela distingue `recovered`, `blocked_requires_intervention` e `known_gap_f6_7`.
9. A transação knowledge permanece intocada nesta tarefa. O falso `COMMITTED` é evidência negativa
   obrigatória para a F6.7 e impede o gate de saída da Fase 6, mas não invalida os oito contratos
   efetivamente pertencentes à F6.6.

## Escopo congelado

### Produto permitido

- `src/ai_engineering_harness/workspace/git_worktree.py`, exclusivamente para recuperar/revalidar a
  criação já iniciada e tornar retry/ACTIVE idempotentes sem cleanup automático.

### Testes e documentação permitidos

- `tests/unit/test_git_worktree.py`, para interrupção após efeito Git, retry, ACTIVE idempotente e
  inconsistência fail-closed;
- `tests/unit/test_phase7.py`, somente para congelar a evidência negativa knowledge que a F6.7 terá
  de substituir;
- `docs/user_guide.md`, para a matriz operacional dos nove pontos;
- `README.md`, `TASK.md`, `docs/tasks/README.md`, este dossiê,
  `tests/unit/test_documentation.py` e `tests/unit/test_task_ledger.py`, somente para estado e
  sentinelas da tarefa.

### Reuso obrigatório sem edição

- lifecycle/FSM, `GraphExecutor`, context assembler, planner, tool loop, state storage, journal,
  approval, promotion manager, evidence manager, redaction, locking/fencing e seus testes promovidos
  são autoridades existentes e devem ser apenas executados pela matriz de aceite.

### Fora de escopo

- corrigir `KnowledgeTransactionManager`, integrar knowledge ao lifecycle, alterar `current.json`,
  staging, pointer swap, lock/fencing ou retenção — ownership exclusivo da F6.7;
- criar API/CLI de fault injection em produção, aceitar nomes de failpoint vindos do usuário ou
  fabricar sucesso/resposta simulada;
- alterar execution record, event, evidence, approval, worktree reference ou outro schema persistido;
- mudar provider, tool, terminal, MCP, context, planner, promotion/rollback, dependências, lockfile,
  defaults, graphs, package version ou CI;
- skip/xfail, relaxamento de validação, cleanup forçado, push, PR, merge, tag remota, force/bypass ou
  remoção de refs.

## Critérios de aceite congelados

1. Testes dedicados reproduzem interrupção entre o efeito Git e `ACTIVE`, comprovam uma única branch
   e um único path, retry para `ACTIVE` e nova chamada idempotente sem segundo efeito.
2. Estado `CREATING` parcialmente inconsistente e identidade/base divergente falham fechados sem
   remover artefatos.
3. A matriz selecionada inclui explicitamente contexto pós-save, model response com/sem outcome,
   tool call antes do handler/par parcial, append atômico, candidate/CAS, aprovação/CAS e promoção
   antes/depois do efeito; todos os testes existentes citados permanecem verdes.
4. O teste de caracterização knowledge comprova `PREPARED → COMMITTED` sem staging/pointer e o guia
   classifica o caso como `known_gap_f6_7`, nunca como recovery válido.
5. O guia contém exatamente nove checkpoints e os cinco campos normativos para cada um; model/tool
   ambíguos são `blocked_requires_intervention`, não reexecutados.
6. Documentação/ledger, suíte F6 aplicável e E2E de promoção permanecem verdes.
7. Antes de `COMPLETED_LOCAL`: suíte completa confinada; Ruff global; mypy em todo `src`;
   `compileall`; `git diff --check`; build de sdist/wheel e smoke oficial offline; diff final restrito
   ao allowlist.

Os comandos exatos usam o runtime congelado, `-p no:cacheprovider` e `--basetemp` fora do checkout.
Qualquer necessidade de novo produto, schema, dependência, cleanup automático ou alteração knowledge
reabre o gate como `BLOCKED` e exige recongelamento antes de continuar.

## Rollback

- O checkpoint `checkpoint/f6.6-ready` preserva a base documental anterior à implementação.
- Mudanças de produto serão commits isolados e revertíveis com `git revert`; nunca usar
  `git reset --hard`, force, descarte amplo ou sobrescrita de trabalho preexistente.
- O rollback do worktree restaura apenas os hunks de `git_worktree.py`; não remove worktrees,
  branches, referências ou diretórios existentes.
- Depois de revert, repetir o probe que observa `CREATING`/colisão, a baseline selecionada de 13
  testes, documentação/ledger e `git diff --check`.
- Evidência negativa knowledge é preservada; removê-la ou reclassificá-la como sucesso invalida o
  gate e bloqueia a promoção.

## Checklist de liberação

- [x] regras, painel, plano 1.1–1.2, Fase 6 e dossiê F6.5 lidos integralmente;
- [x] PR #78, head final, CI do PR, merge e CI pós-merge comprovados terminalmente;
- [x] baseline Git, workspace, runtime e executor registrados;
- [x] branch exclusiva criada da `main` certificada e workspace limpo;
- [x] baseline selecionada `13 passed, 183 deselected in 43.84s`;
- [x] lacuna worktree reproduzida como `CREATING` + `WorktreeCollisionError`;
- [x] falso recovery knowledge reproduzido sem staging/pointer;
- [x] escopo, contrato e aceite congelados;
- [x] rollback congelado e não destrutivo;
- [x] autorização explícita para revisão e implementação local;
- [x] commit documental `2d01cca` e tag local `checkpoint/f6.6-ready`;
- [x] implementação e testes no allowlist, commit de produto `1d54674`;
- [x] certificação integral e checkpoint `COMPLETED_LOCAL`.

## Diário de execução

- `2026-08-16T13:02:13-03:00` — autorização nominal consumida para revisar o escopo e iniciar a
  implementação local em nova branch; nenhum efeito remoto foi autorizado.
- `main` avançou somente por fast-forward para `6386816`; a branch
  `task/f6.6-recovery-failures` nasceu desse SHA sem upstream.
- A baseline selecionada passou 13 testes. Os probes confirmaram recovery ausente após criação do
  worktree e falso commit knowledge; este último permanece reservado à F6.7.
- O gate torna-se `READY` somente para o allowlist acima. A F6.7 e o gate de saída da fase continuam
  bloqueados.
- `2026-08-16T14:28:28-03:00` — produto concluído no commit
  `1d5467457cf99c4ee34d69000630de1b1aa0900b`. Retry de worktree revalida identidade completa,
  `git-common-dir`, base, branch, path, HEAD e limpeza; recupera somente nenhum efeito ou efeito
  completo, e bloqueia metade, repositório estrangeiro ou divergência sem cleanup.
- Testes dedicados de worktree: `29 passed in 35.10s`; matriz selecionada dos nove checkpoints:
  `24 passed in 56.83s`; certificação focada: `234 passed in 332.99s`.
- A full R1 preservou a evidência ambiental `1029 passed, 5 skipped, 6 subtests passed` e uma falha
  de permissão em `AppData\\Local` no `test_sandbox_provider_by_platform`. O caso passou isolado
  (`1 passed in 1.04s`) após confinar `LOCALAPPDATA`; a full R2 autoritativa encerrou
  `1030 passed, 5 skipped, 6 subtests passed in 765.28s`.
- Ruff global, mypy em `110` arquivos, compileall e `git diff --check` passaram. O build isolado
  gerou sdist (`325588` bytes) e wheel (`371627` bytes) fora do checkout; o smoke oficial offline
  confirmou metadata, pacote e CLI `0.1.0`, sem bytecode dependente da máquina.
- Documentação e ledger finais passaram `32 passed in 0.76s`; `TASK.md` permaneceu limitado a
  `270` linhas.
- O guia contém exatamente nove checkpoints e os cinco campos normativos. Knowledge permanece
  `known_gap_f6_7`: o teste congela `PREPARED → COMMITTED` sem staging/pointer e não altera
  `KnowledgeTransactionManager`.
- O gate passa a `COMPLETED_LOCAL / PROMOTION_PENDING`. Não houve push, PR, merge, tag remota,
  force/bypass, cleanup ou remoção de ref.
