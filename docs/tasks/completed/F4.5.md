# F4.5 — Normalização fail-closed dos gates de verificação

> **Gate:** `READY`
> **Lifecycle:** `PROMOTED`
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-11T00:31:23-03:00`
> **Branch:** `task/f4.5-normalize-gates`
> **Python:** `.\.venv\Scripts\python.exe` — 3.12.13
> **Baseline:** `main == origin/main == 362407f4abd3aa98ae37278fb243d6eb73f11681`
> **Checkpoint:** `checkpoint/f4.5-ready` designa o commit documental deste gate
> **Implementação:** `bfb70fc216900e610fd80ffe1fd2da89382ce1b0`
> **Promoção:** PR [#42](https://github.com/Wf-ops1/Harnessinfra/pull/42), head final `9e8dfe80a8aaf0bcf4180866fb6e40eb117b0fc6`, merge `4ae0de798607cf4fec13c0469fddb93d8024ead5`
> **Decisão arquitetural:** [DEC-015](../../decisions/DEC-015-composicao-canonica-fase4.md)

O gate começou `BLOCKED`. Ele passa a `READY` somente porque problema, baseline, escopo, aceite,
rollback, responsabilidade, runtime e verdade temporal estão congelados abaixo. Nenhum arquivo de
produto foi alterado antes deste registro e de seu checkpoint Git.

## Problema comprovado

No baseline promovido, a policy distribuída declara `tests`, enquanto o evaluator conhece
`unit_test`. O conjunto oficial também inclui `security_scan` quando habilitado, mas o evaluator não o
reconhece. `GateRunner` ignora qualquer gate sem argv e inicializa `all_passed=True`; por isso uma
suíte vazia, um ID desconhecido e o próprio ID `tests` da policy terminam como sucesso `0/0`.

Reprodução executada sem alterar o repositório:

```text
command: .\.venv\Scripts\python.exe -c "... policy/evaluator/GateRunner ..."
exit: 0
policy_ids= ['typecheck', 'lint', 'tests', 'build']
evaluator_unit_test= ('pytest',)
evaluator_tests= None
evaluator_security_scan= None
[]          -> all_passed=True, total_gates=0, passed_gates=0
['unknown'] -> all_passed=True, total_gates=0, passed_gates=0
['tests']   -> all_passed=True, total_gates=0, passed_gates=0
```

Locais comprovados:

- `src/ai_engineering_harness/defaults/policies/verification_policy.yaml` usa `tests`;
- `src/ai_engineering_harness/verification/evaluator.py` usa `unit_test` e retorna `None` quando não
  encontra ID/comando;
- `src/ai_engineering_harness/verification/gate_runner.py` usa `continue` nesse caso e aceita a lista
  resultante vazia;
- DEC-015 e o ledger da F4.3 preservam esses falsos sucessos para F4.5–F4.7.

## Baseline conhecido

| Evidência | Resultado observado |
|---|---|
| Workspace | `C:\Users\walla\OneDrive\Desktop\ai-engineering-harness` |
| Git inicial | `.git` presente; `main...origin/main`; workspace limpo; zero ahead/behind |
| SHA promovido | `HEAD == main == origin/main == 362407f4abd3aa98ae37278fb243d6eb73f11681` |
| Promoção anterior | PR administrativo #41 incorporado pelo merge commit `362407f`; branch remota preservada |
| CI do SHA | run `31455148050`, evento `push`, 11/11 success: package 2/2, quality 4/4, tests 4/4 e `CI required` |
| Baseline focado | `53 passed`; primeira rodada teve somente 11 erros de setup por `PermissionError` no temp global e foi repetida integralmente com `--basetemp` isolado |
| Branch F4.5 | `task/f4.5-normalize-gates`, criada diretamente do SHA promovido e depois publicada com upstream homônimo |
| Runtime | `.\.venv\Scripts\python.exe` — Python 3.12.13 |
| Trabalho preexistente | nenhum; nenhuma tag remota publicada e nenhuma ref excluída |

## Contrato corretivo congelado

1. Os únicos IDs canônicos são `typecheck`, `lint`, `unit_test`, `build` e `security_scan`.
2. A policy distribuída usa `typecheck`, `lint`, `unit_test` e `build`; `security_scan` só aparece
   quando explicitamente habilitado. O alias legado `tests` é inválido, não é convertido em silêncio.
3. O contrato tipado da policy rejeita ID desconhecido, lista vazia, duplicata e ausência de ao menos
   um gate bloqueante antes de produzir policy resolvida ou artefato compilado.
4. Policy, planner e evaluator compartilham o mesmo conjunto canônico; não existem listas paralelas
   divergentes aceitas em runtime.
5. `GateRunner` rejeita antes de qualquer subprocesso uma seleção vazia, desconhecida, duplicada ou
   sem comando para a linguagem. Nenhuma dessas condições pode produzir `all_passed=True` ou `0/0`.
6. Gates canônicos mapeados preservam execução por `argv`, `shell=False`, cwd confinado, ambiente
   seletivo, timeout, limite/redação de saída e resultado reprovado quando comando/ferramenta falha.
7. `security_scan` reconhecido mas ainda sem comando efetivo deve bloquear se for habilitado; F4.5 não
   inventa detecção/comando que pertence à F4.6.
8. F4.5 não persiste status/argv/cwd/tempo/exit/output/digest, não guarda `COMPLETED` e não cria retry;
   esses efeitos permanecem exclusivamente em F4.7/F4.8 conforme DEC-015.

## Escopo congelado

### Permitido

- `src/ai_engineering_harness/contracts/policies.py` e export estritamente necessário em
  `contracts/__init__.py` — conjunto tipado canônico e validação fail-closed da policy;
- `src/ai_engineering_harness/verification/evaluator.py`, `gate_runner.py` e `__init__.py` — consumo do
  conjunto canônico e erro tipado antes de qualquer falso sucesso;
- `src/ai_engineering_harness/defaults/policies/verification_policy.yaml` — `tests → unit_test`;
- `tests/unit/test_phase5.py`, `test_policy_registry.py`, `test_planner.py` e consumidores F4.4
  estritamente afetados — convergência, positivos e negativos determinísticos;
- `TASK.md`, `README.md`, `docs/tasks/README.md`, este dossiê, guias/auditorias que ainda descrevem
  `0/0`, e `tests/unit/test_task_ledger.py`, `test_documentation.py`, `test_encoding.py` — estado e
  documentação honestos.

### Proibido

- alterar `ExecutionRecord`, FSM, storage, `GraphExecutor`, lifecycle de conclusão ou schemas/digests
  serializados;
- implementar `StackDetector`, descobrir ferramentas/configuração ou resolver comandos efetivos F4.6;
- persistir resultados, criar status `ERROR_PREREQUISITE`, guardar `COMPLETED` ou alterar digest do
  commit — F4.7;
- implementar repair loop, retry, orçamento, promoção F3.7 ou qualquer F4.8+;
- adicionar dependências, lockfile, workflow/CI, backend, processo, serviço, mock ou sucesso sintético;
- publicar branch/tag, abrir/mesclar PR, excluir ref, force-push ou bypass sem autorização nominal nova.

Novo arquivo de produto, dependência, efeito externo ou mudança material de critério reabre o gate e
exige recongelamento antes de continuar. Critério falho não pode ser removido, skipado ou relaxado.

## Critérios de aceite congelados

```yaml
acceptance:
  focused:
    command: >-
      .\.venv\Scripts\python.exe -m pytest
      tests/unit/test_phase5.py tests/unit/test_policy_registry.py
      tests/unit/test_planner.py tests/unit/test_planning_lifecycle.py -q
    expected: >-
      IDs canônicos convergem; default usa unit_test; policy vazia/desconhecida/duplicada falha;
      runner vazio/desconhecido/duplicado/sem comando falha antes de efeitos; gates válidos continuam
  phase4_compatibility:
    command: >-
      .\.venv\Scripts\python.exe -m pytest
      tests/unit/test_context_lifecycle.py tests/unit/test_planner.py
      tests/unit/test_planning_lifecycle.py tests/unit/test_execution_lifecycle.py
      tests/unit/test_graph_executor.py -q
    expected: "F4.3/F4.4 e lifecycle promovidos permanecem verdes com unit_test canônico"
  documentation:
    command: >-
      .\.venv\Scripts\python.exe -m pytest
      tests/unit/test_task_ledger.py tests/unit/test_documentation.py tests/unit/test_encoding.py -q
    expected: "painel, dossiê ativo, guias, links, Markdown e UTF-8 coerentes"
  regression:
    command: ".\.venv\Scripts\python.exe -m pytest tests/unit tests/e2e -q"
    expected: "suíte integral verde; somente skips live opt-in preexistentes"
  quality:
    command: >-
      .\.venv\Scripts\python.exe -m mypy src;
      .\.venv\Scripts\python.exe -m mypy --platform linux src;
      .\.venv\Scripts\python.exe -m ruff check src compiler tests;
      .\.venv\Scripts\python.exe -m compileall -q src compiler tests;
      git diff --check
    expected: "todos exit 0, sem suppression ou relaxamento"
  package_ci_parity:
    command: >-
      .\.venv\Scripts\python.exe -m build;
      .\.venv\Scripts\python.exe tests/ci/smoke_wheel.py
    expected: "sdist/wheel e smoke isolado verdes; CI remota aguarda publicação autorizada"
  scope:
    command: "git diff --name-only checkpoint/f4.5-ready e auditoria das fronteiras proibidas"
    expected: "somente a allowlist F4.5; F4.6+, deps, CI e schemas persistidos intactos"
```

Falha em qualquer critério mantém `ACTIVE` e bloqueia publicação. Sucesso local permite somente
`COMPLETED_LOCAL / PROMOTION_PENDING`; não autoriza push, PR, merge nem início da F4.6.

## Rollback

### Gatilhos

- qualquer seleção vazia, desconhecida, duplicada ou sem comando voltar a produzir sucesso/`0/0`;
- policy e evaluator aceitarem conjuntos diferentes ou `tests` ser convertido silenciosamente;
- gate válido deixar de executar, erro ocorrer depois de efeito evitável ou boundary F4.6+ ser alterada;
- aceite, regressão, quality, package/smoke, documentação ou auditoria de escopo falhar.

### Procedimento não destrutivo

Parar e preservar diff/saídas. Antes do commit de implementação, inverter somente os hunks F4.5 com
`apply_patch`. Depois de commit, usar `git revert` apenas nos commits exclusivos desta branch. Nunca
usar `git reset --hard`, `git clean`, checkout amplo ou apagar trabalho/estado do usuário.

### Verificação pós-rollback

Confirmar `git status --short --branch`, `checkpoint/f4.5-ready`, `git diff --check` e zero diff de
produto contra o checkpoint; repetir reprodução `0/0`, aceite focado, compatibilidade F4.3/F4.4,
documentação, regressão, quality e package/smoke. O falso sucesso reaparecer comprova o retorno ao
baseline, mas mantém F4.6 bloqueada.

## Checklist de liberação

```text
[x] problema reproduzido com policy/evaluator e três sucessos 0/0
[x] baseline Git, upstream e CI 11/11 do SHA exato observados
[x] escopo permitido/proibido e contrato fail-closed congelados
[x] critérios focados, compatibilidade, regressão, quality, package e escopo congelados
[x] rollback não destrutivo, executor, runtime e horário registrados
[x] F4.6+, F3.7, dependências, CI, merge e refs permanecem fora do gate de produto
```

## Evidências de execução

### Implementação observada

`VerificationGateId` e `CANONICAL_VERIFICATION_GATE_IDS` passaram a ser a única taxonomia tipada da
policy e do evaluator. `VerificationPolicySpec` rejeita lista vazia, ID fora do `Literal`, duplicata e
ausência de gate bloqueante. O default substitui `tests` por `unit_test`; `security_scan` permanece
canônico, porém não habilitado pelo default.

`GateRunner` congela a seleção e valida lista vazia, tipo, duplicata, ID canônico e disponibilidade de
argv para a linguagem antes de chamar `_adapter_for()` ou abrir subprocesso. Assim, `unknown`, `tests`,
`security_scan` ainda sem comando e linguagem sem mapping geram `VerificationConfigurationError` em
vez de serem ignorados. Gates mapeados preservam o terminal tipado e o contrato de resultado atual.

Nenhum comando de security foi inventado: se `security_scan` for habilitado antes da F4.6, a ausência
de mapping bloqueia. Nenhum status persistido, digest, guard de `COMPLETED`, retry ou alteração de CLI
foi introduzido.

### Validações observadas

| Critério | Resultado |
|---|---|
| Checkpoint READY | commit/tag local `42c64c420e923d7aa03e1a931a5870c022d06fd6` / `checkpoint/f4.5-ready`, antes de qualquer arquivo de produto |
| Baseline focado | `53 passed`; rodada inicial teve apenas 11 erros de setup pelo temp global bloqueado |
| Aceite focado F4.5 | `64 passed` |
| Compatibilidade F4.3/F4.4 | `79 passed` |
| Documentação/ledger/encoding | `25 passed, 6 subtests passed` |
| Regressão no sandbox | `713 passed, 2 skipped, 6 subtests passed`; única falha foi `PermissionError` no path contratual de AppData |
| Regressão integral externa | `714 passed, 2 skipped, 6 subtests passed`; skips somente Serena/modelo live opt-in preexistentes |
| mypy Windows | 105 arquivos, zero issues; duas notas preexistentes de corpos não tipados em `lease_manager.py` |
| mypy Linux | 105 arquivos, zero issues; mesmas duas notas preexistentes |
| Ruff / compileall / diff | todos verdes; `git diff --check` em exit 0 |
| Build | sdist e wheel `0.1.0` gerados pelo build PEP 517 isolado; primeira tentativa foi bloqueada no bootstrap temporário do sandbox |
| Smoke da wheel | metadata/package/CLI `0.1.0`; import originado do cache isolado do `uv`, fora do workspace |
| Escopo | 17 arquivos na allowlist; zero diff em runtime, persistence, detector F4.6, CLI, schemas persistidos, deps, lockfile, compiler ou CI |

Os testes que usam `tmp_path` foram repetidos com `--basetemp` no root temporário autorizado porque o
sandbox não podia enumerar o temp global. A regressão foi repetida integralmente fora do sandbox para
o teste existente de `SandboxProvider` acessar AppData. Build e smoke também foram repetidos fora do
sandbox por bootstrap/leitura da wheel; o smoke recebeu somente o `uv 0.11.32` versionado no `PATH` do
processo. Essas adaptações alteraram confinamento ambiental, nunca código, teste ou expectativa.

### Estado publicado observado

| Evidência | Resultado |
|---|---|
| Branch | `task/f4.5-normalize-gates`, acompanhando `origin/task/f4.5-normalize-gates` |
| Commit READY | `42c64c420e923d7aa03e1a931a5870c022d06fd6` |
| Commit funcional | `bfb70fc216900e610fd80ffe1fd2da89382ce1b0` |
| Aceite local | `a77b4d9890a83a498c5f70db7efdcec92d92baed` publicado como head inicial |
| PR | [#42](https://github.com/Wf-ops1/Harnessinfra/pull/42), aberto e mergeável para `main` com 3 commits e 19 arquivos |
| CI inicial | run `31457756495`, evento `pull_request`, observado com 10 jobs em andamento no head inicial |
| Fronteira remota | branch e PR publicados; nenhuma tag remota, merge ou exclusão de ref |
| Próxima tarefa | F4.6/F3.7 não iniciadas e bloqueadas até promoção/reconciliação da F4.5 |

A autorização nominal publicou a branch e abriu o PR único da F4.5. Este registro documental produz
um head posterior a `a77b4d9`, portanto o run inicial `31457756495` não pode certificar o head final.
O lifecycle passa a `PR_OPEN / CHECKS_PENDING`; merge exige todos os checks do novo head verdes e
autorização nominal nova. Tag, exclusão de ref e início da F4.6/F3.7 continuam não autorizados.

## Reconciliação administrativa pós-merge — DEC-014

> **Observado em:** `2026-08-11T01:31:03-03:00`
> **Lifecycle certificado:** `PROMOTED`
> **Branch administrativa:** `docs/promote-f4.5`, criada de `main == origin/main == 4ae0de7`
> **Estado da reconciliação:** `LOCAL_READY / PUBLICATION_PENDING`

O usuário autorizou nominalmente o merge do PR #42. Antes da ação, o PR foi revalidado aberto, no
head final `9e8dfe8`, com 11/11 checks verdes e sem conflitos. O merge commit foi observado em `main`,
seguido pela CI de `push` integralmente verde no mesmo SHA.

| Evidência | Resultado observado |
|---|---|
| PR de implementação | [#42](https://github.com/Wf-ops1/Harnessinfra/pull/42), base `main`, head final `9e8dfe80a8aaf0bcf4180866fb6e40eb117b0fc6`, quatro commits e 19 arquivos |
| Checks do head final | run `31457935429`, evento `pull_request`; 11/11 success, incluindo `CI required` |
| Merge | merge commit `4ae0de798607cf4fec13c0469fddb93d8024ead5`, com pais `362407f4abd3aa98ae37278fb243d6eb73f11681` e `9e8dfe80a8aaf0bcf4180866fb6e40eb117b0fc6` |
| CI pós-merge | run `31458482033`, evento `push`, no SHA exato do merge; 11/11 success em 2/2 package, 4/4 quality, 4/4 tests e `CI required` |
| Sincronização | `main == origin/main == 4ae0de798607cf4fec13c0469fddb93d8024ead5` antes da branch administrativa |
| Fronteira remota | branch de implementação preservada; `checkpoint/f4.5-ready` permanece somente local; nenhuma tag publicada ou ref excluída |
| Reconciliação | branch local `docs/promote-f4.5`, sem upstream; publicação e PR administrativo ainda não autorizados |

A F4.5 está promovida e arquivada, sem dossiê de implementação ativo. F4.6+ e F3.7 não foram
iniciadas e permanecem bloqueadas até o PR administrativo desta reconciliação ser publicado,
incorporado e receber CI pós-merge verde em `main`.

### Escopo, aceite e rollback administrativos

```yaml
acceptance:
  documentation: >-
    .\.venv\Scripts\python.exe -m pytest
    tests/unit/test_task_ledger.py tests/unit/test_documentation.py tests/unit/test_encoding.py -q
  quality: >-
    .\.venv\Scripts\python.exe -m ruff check
    tests/unit/test_task_ledger.py tests/unit/test_documentation.py; git diff --check
  state: >-
    F4.5 PROMOTED/arquivada; PR #42, head 9e8dfe8, merge 4ae0de7 e runs
    31457935429/31458482033 preservados; nenhum dossiê de implementação ativo
  scope: >-
    somente README, painel, índice, movimentação/certificação do dossiê e duas regressões
    documentais; produto, dependências, schemas, defaults e CI byte-idênticos a 4ae0de7
rollback:
  trigger: "qualquer teste, link, encoding, fato remoto ou auditoria de escopo falhar"
  procedure: "reverter somente commits desta branch com git revert; nunca reset destrutivo"
  verify: "repetir suíte documental, Ruff, git diff --check e auditoria de fronteiras"
```

Checklist da reconciliação:

```text
[x] PR final e 11/11 checks verdes observados no head exato
[x] merge commit, dois pais e branch remota de implementação preservada
[x] CI push pós-merge verde no SHA exato de main
[x] main local limpa e sincronizada antes da branch administrativa
[x] escopo exclusivamente documental, aceite e rollback registrados
[x] nenhuma tag publicada e nenhuma ref excluída
[x] F4.6+/F3.7 não iniciadas
[x] validação documental local
[ ] publicação, PR e merge administrativos — não autorizados
```

### Validação da reconciliação local

A primeira rodada documental concluiu `5 failed, 20 passed, 6 subtests passed`: o painel precisava
preservar a frase canônica “nenhuma tarefa ativa”, duas regressões ainda exigiam o snapshot temporal
pré-merge e a referência histórica F4.C1 continuava indevidamente duplicada no painel corrente. Após
o ajuste, duas rodadas consecutivas concluíram `1 failed, 24 passed, 6 subtests passed`; a primeira
encontrou o run inicial da F4.5 ainda exigido no README já promovido, e a segunda encontrou uma frase
antiga da reconciliação F4.C1 no índice. Esses contratos temporais foram atualizados sem remover
evidência do dossiê histórico nem enfraquecer critérios.

A recertificação final concluiu `25 passed, 6 subtests passed`. O aviso único decorreu somente da
impossibilidade de gravar `.pytest_cache` no sandbox e não alterou o resultado.

| Critério | Resultado final observado |
|---|---|
| Documentação, ledger e encoding | `25 passed, 6 subtests passed` |
| Ruff nos testes documentais alterados | `All checks passed!` |
| Integridade do diff | `git diff --check` verde; apenas avisos de conversão LF/CRLF do checkout |
| Escopo | somente `README.md`, `TASK.md`, índice, movimentação/certificação do dossiê e duas regressões documentais |
| Fronteiras proibidas | nenhum diff contra `4ae0de7` em `src/`, `compiler/`, `.github/`, `pyproject.toml` ou `uv.lock` |

A reconciliação está certificada localmente, sem tag e sem upstream. Publicação, abertura do PR
administrativo, merge, exclusão de refs e início da F4.6+/F3.7 continuam não autorizados.
