# F3.8 — Edição real confinada e Serena MCP explícito

> **Gate:** `READY`
> **Lifecycle:** `PROMOTED`
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-08T22:11:02-03:00`
> **Promoção certificada em:** `2026-08-09T02:00:28-03:00`
> **Runtime:** `.\.venv\Scripts\python.exe` — Python 3.12.13
> **uv:** `.\build\f0.6-tools\uv\bin\uv.exe` — 0.11.32
> **Branch:** `task/f3.8-real-editing`
> **Fronteira:** edição e leitura locais confinadas, terminal F3.5 injetado, Git somente leitura e
> Serena MCP configurado explicitamente; lifecycle/CLI, promoção F3.7 e fallback automático permanecem bloqueados.

## Problema comprovado

| Evidência reproduzível | Resultado observado |
|---|---|
| `.\.venv\Scripts\python.exe -c "... SerenaAdapter().edit_file_semantic(...) ..."` | `{'result': True, 'exists': True, 'size': 0}`: sucesso declarado, arquivo ausente criado vazio e nenhuma mudança aplicada |
| `src/ai_engineering_harness/tools/adapters/serena.py` | `edit_file_semantic()` executa somente `mkdir`/`touch` e retorna `True`; não há transporte, handshake, discovery, capability ou efeito validado |
| `ToolRouter(allowed_tools=(...), registrations={})` e regressões F3.C2 | o registry operacional padrão permanece vazio; capabilities declaradas não possuem handlers de leitura/edição |
| `src/ai_engineering_harness/tools/adapters/git.py` | usa a API de shell string que a F3.5 tornou fail-closed e ainda contém SHA sintético `uncommitted` |
| README e plano F3.8 | exigem `read_file`, `list_files`, `search_text`, `apply_patch`, `run_command`, Git controlado e Serena MCP real ou fallback local explícito |

O caminho atual consegue afirmar uma edição inexistente e criar um artefato vazio fora de qualquer
contrato de patch. Não existe factory opt-in que ligue as primitivas já promovidas de path guard,
worktree e terminal ao `ToolRouter`. A F3.8 deve entregar essa superfície como primitiva operacional
injetável sem antecipar a ligação automática do lifecycle, a promoção Git ou uma disponibilidade MCP
que não tenha sido observada.

## Baseline conhecido

```yaml
baseline:
  branch: "task/f3.8-real-editing"
  parent_main: "fd49310ddca91e10381a08a7f456fe9ab03d3636"
  origin_main_observed: "fd49310ddca91e10381a08a7f456fe9ab03d3636"
  status_before_branch: "main limpa e sincronizada; rev-list 0 0"
  status_after_branch: "branch exclusiva limpa, criada diretamente do baseline"
  upstream_ci:
    run: "31287059584"
    event: "push"
    branch: "main"
    head: "fd49310ddca91e10381a08a7f456fe9ab03d3636"
    result: "11/11 completed/success, incluindo CI required; revalidado no GitHub às 22:10 -03:00"
  previous_promotion: "F3.5 PROMOTED; PR #27, merge b6a4a24, run pós-merge 31285547886"
  administrative_baseline: "PR #28, merge fd49310, run pós-merge 31287059584"
  checkpoint: "checkpoint/f3.8-ready aponta para o primeiro commit documental deste gate"
  python: ".venv Python 3.12.13 com dependências do projeto importáveis"
  uv: "0.11.32"
```

A consulta `git ls-remote` foi bloqueada pela rede do sandbox neste turno e não foi convertida em
sucesso. O SHA remoto e a CI foram revalidados na sessão autenticada do GitHub: evento `push`, branch
`main`, head exato `fd49310`, status `Success`, matrizes package 2/2, quality 4/4, tests 4/4 e
`CI required` verde. Git local comprovou `HEAD == origin/main` e divergência `0 0` antes da branch.

## Dependências revalidadas

1. F3.4 fornece `PathGuard` fail-closed com raiz explícita, resolução canônica, bloqueio de travessia,
   escrita em `.git` e limites de leitura/escrita.
2. F3.6 fornece `ProvisionedWorktree` com `PathGuard` cuja raiz é o worktree Git externo validado. A
   factory F3.8 recebe esse guard ou o provisionado explicitamente; nunca infere raiz por `cwd`.
3. F3.5 fornece `TerminalAdapter` por `argv`, executável absoluto autorizado, ambiente seletivo,
   timeout da árvore, saída limitada e redigida. F3.8 somente o adapta ao schema JSON da tool.
4. F3.C2 fornece `ToolRouter`, deny-wins, preflight do batch e write-ahead/outcome durável no tool loop.
   A F3.8 adiciona registrations opt-in; não relaxa policy nem o recorder obrigatório.
5. DEC-013 exige F3.4 + F3.6 antes da edição. DEC-014 exige novo PR administrativo somente depois de
   eventual merge/CI desta implementação; ela não autoriza publicação ou merge agora.
6. O SDK oficial MCP suporta cliente stdio e Streamable HTTP; Serena documenta lançamento por comando
   ou servidor HTTP. A dependência será `mcp>=1.26,<2`, compatível com o protocolo usado pela linha
   Serena 1.x e sem implementar JSON-RPC próprio.

## Contrato técnico congelado

1. `LocalEditingAdapter` exige `PathGuard` explícito e oferece `read_file`, `list_files`, `search_text`
   e `apply_patch`; nenhum método aceita raiz ou `cwd` implícito.
2. Leitura aceita somente arquivo regular UTF-8 estrito já existente, revalida o guard imediatamente
   antes do I/O e devolve path relativo, conteúdo e SHA-256 reais dentro do limite F3.4.
3. Listagem aceita somente diretório existente confinado, não percorre symlink para fora, possui
   profundidade e número máximo de entradas positivos, ordenação determinística e paths relativos.
4. Busca é literal e UTF-8, limitada por arquivos/resultados/bytes, percorre somente os paths que o
   guard revalidar e devolve arquivo, linha, coluna e trecho limitados; erro não vira lista vazia.
5. `apply_patch` aceita um único target e um patch unified-diff estrito. Multi-arquivo, rename, delete,
   binário, hunk malformado, contexto divergente, newline ambíguo ou trailing data falham tipados.
6. Arquivo existente exige o `expected_sha256` exato; criação exige ausência comprovada, digest nulo,
   parent existente/confinado e conteúdo não vazio. Patch sem mudança é erro, nunca sucesso.
7. A escrita verifica tamanho, proíbe `.git`, revalida target/parent imediatamente antes do efeito,
   publica por arquivo temporário no mesmo diretório + `fsync` + `os.replace` e devolve digest/path
   reais. Nenhum diretório é criado implicitamente.
8. `run_command` somente converte payload tipado em `CommandRequest` e chama um `TerminalAdapter`
   injetado. Não há shell string, resolução por PATH, herança de ambiente ou bypass do guard.
9. As operações Git deste gate são somente `git_status` e `git_diff`, por argv estático através do
   mesmo terminal seguro. Commit, add, checkout, reset, clean, branch, worktree, cherry-pick, revert,
   push e SHA fallback pertencem a F3.7/F6 e permanecem proibidos.
10. `SerenaAdapter` não possui endpoint default nem fallback silencioso. Ele recebe uma configuração
    explícita stdio ou Streamable HTTP, timeout finito, `PathGuard` do worktree e allowlist fixa de
    ferramentas semânticas de edição.
11. No stdio, o executável deve ser absoluto/existente, argumentos são argv sem shell e o projeto
    autorizado é fornecido explicitamente. No HTTP, a sessão deve inicializar, descobrir capabilities,
    ativar a raiz exata e comprovar o projeto ativo antes de qualquer edição.
12. Somente `replace_symbol_body`, `insert_before_symbol`, `insert_after_symbol` e `replace_content`
    podem ser chamados por `serena_edit`. `relative_path` é validado/injetado pelo adapter, não aceito
    duplicado dentro dos argumentos do modelo.
13. Antes e depois do call MCP, o arquivo-alvo é revalidado. Resultado MCP com erro, conexão ausente,
    capability faltante, raiz divergente, arquivo inalterado ou efeito fora do limite falha tipado e
    nunca retorna saudável/sucesso.
14. A factory opt-in registra schemas JSON exatos para as oito tools deste gate. Registration ausente
    continua produzindo `ToolUnavailableError`; policy compilada/deny-wins continua soberana.
15. O adapter local é fallback somente quando o chamador o selecionar explicitamente. Falha Serena
    não troca automaticamente de backend nem reaplica uma edição por outro caminho.
16. Nenhuma configuração default, grafo, CLI ou lifecycle passa a executar essas tools neste gate. A
    entrega é injetável/testável; sua ligação automática exige gate próprio para não misturar workspace,
    executor, configuração efetiva e promoção.

## Escopo congelado

### Permitido

- `TASK.md`, `README.md`, `docs/agentic_operating_model.md`,
  `docs/harness_architecture_spec.md`, `docs/tasks/README.md`, este dossiê e regressões documentais;
- `pyproject.toml` e `uv.lock` — somente dependência oficial `mcp>=1.26,<2` e resolução resultante;
- `src/ai_engineering_harness/tools/adapters/local_editing.py` — I/O local, busca e patch atômico;
- `src/ai_engineering_harness/tools/adapters/serena.py` — cliente MCP real e falhas tipadas;
- `src/ai_engineering_harness/tools/adapters/__init__.py` — exports estritamente F3.8;
- `src/ai_engineering_harness/tools/operational.py` — schemas/handlers opt-in confinados;
- `src/ai_engineering_harness/tools/__init__.py` — exports da factory F3.8;
- `tests/unit/test_local_editing_adapter.py`, `tests/unit/test_serena_adapter.py`,
  `tests/unit/test_operational_tools.py` e fixture MCP sob `tests/fixtures/`;
- `tests/unit/test_phase4.py`, `tests/unit/test_public_module_imports.py`,
  `tests/unit/test_task_ledger.py` e `tests/unit/test_documentation.py` somente para remover claims
  legados e fixar a nova fronteira.

### Proibido

- `.github`, schemas/contratos, providers, model router, tool loop, recorder, FSM, lifecycle, CLI,
  compiler, defaults, policies, grafos, agentes, verification, indexer e doctor;
- alterar `PathGuard`, `ExternalWorktreeManager`, `TerminalAdapter` ou seus contratos promovidos;
- habilitar tool automaticamente, inferir worktree, registrar backend global ou alterar policy;
- Git mutável, candidate commit, promoção, rollback, aprovação, F3.7 ou gate de saída integral da fase;
- instalar/configurar/iniciar Serena no ambiente do usuário, exigir serviço live no gate local ou
  declarar Serena disponível sem handshake e capability reais;
- fallback automático, mock em produção, sucesso vazio/sintético, patch multi-arquivo ou escrita fora
  do worktree;
- novo arquivo, efeito, dependência ou critério fora desta allowlist sem parar e recongelar.

## Critérios de aceite congelados

```yaml
acceptance:
  local_editing:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_local_editing_adapter.py -q
    expected: >-
      leitura/listagem/busca/patch reais; digest e paths verdadeiros; traversal, symlink escape, .git,
      limite, stale digest, patch inválido, multi-arquivo e corrida revalidada falham antes de sucesso
  serena_mcp:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_serena_adapter.py -q
    expected: >-
      transporte MCP real contra servidor de teste, initialize/list/call; stdio/HTTP configurados,
      capability/root/erro/timeout/arquivo inalterado falham explicitamente; live Serena é opt-in
  operational_registry:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_operational_tools.py tests/unit/test_tool_loop.py -q
    expected: >-
      oito schemas/handlers opt-in, JSON nativo, deny-wins e ausência de backend/registration fail-closed
  compatibility:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_path_guard.py tests/unit/test_git_worktree.py
      tests/unit/test_terminal_adapter.py tests/unit/test_public_module_imports.py -q
    expected: "F3.4, F3.6, F3.5 e imports permanecem verdes sem mudança de contrato"
  documentation:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
      tests/unit/test_task_ledger.py tests/unit/test_documentation.py tests/unit/test_encoding.py -q
    expected: "F3.8 é a única ativa e claims distinguem primitiva opt-in de integração automática/live"
  regression:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest tests/unit tests/e2e -q
    expected: "suíte integral verde; somente integração live explicitamente condicionada pode skipar"
  quality:
    command: >-
      mypy src; mypy --platform linux src; Ruff check; compileall src compiler tests;
      uv lock --check; git diff --check
    expected: "todos exit 0; lock consistente e sem relaxamento"
  package:
    command: >-
      .\build\f0.6-tools\uv\bin\uv.exe run python -m build;
      .\build\f0.6-tools\uv\bin\uv.exe run python tests/ci/smoke_wheel.py
    expected: "sdist/wheel gerados e smoke isolado verde com a dependência MCP declarada"
  scope:
    command: "git diff --name-only checkpoint/f3.8-ready e byte-check das fronteiras proibidas"
    expected: "somente allowlist F3.8; runtime/lifecycle/defaults/CI e primitivas promovidas byte-idênticos"
```

Falha em qualquer critério permanece bloqueante. Teste novo pode ampliar evidência, nunca substituir,
remover ou enfraquecer o congelado.

## Rollback

### Gatilhos

- retorno positivo sem mudança/digest real, patch parcial ou conteúdo/contexto divergente aceito;
- escape por path/symlink, escrita em `.git`, excesso de limite ou diretório implícito ocorrer;
- shell, PATH, ambiente herdado, Git mutável ou checkout original receber efeito;
- Serena declarar saúde sem initialize/discovery/root/capability, mudar arquivo fora da raiz ou acionar
  fallback local automaticamente;
- dependência não oficial/adicional, configuração default, lifecycle, CLI, policy, schema ou arquivo
  proibido tornar-se necessário;
- qualquer teste, quality gate, pacote, lock ou auditoria de escopo falhar.

### Procedimento não destrutivo

Parar e preservar saídas, temporários e diff para diagnóstico. Antes de commit, inverter somente os
hunks F3.8 com `apply_patch`. Depois de commit, usar `git revert` nos commits exclusivos desta branch;
nunca usar reset destrutivo, clean, checkout amplo ou sobrescrever trabalho do usuário. Se a correção
exigir ampliar a allowlist/efeito/dependência/critério, registrar R1 neste dossiê, recongelar e criar
novo checkpoint antes de retomar.

### Verificação pós-rollback

`git diff --check`, suíte documental, testes F3.4/F3.5/F3.6/tool-loop e suíte integral devem retornar
ao baseline verde. A reprodução do `SerenaAdapter` sintético pode voltar a existir somente se o gate
for formalmente abandonado; nesse caso o painel deve declarar `BLOCKED`, nunca sucesso.

## Checklist de liberação

```text
[x] baseline Git limpo e sincronizado no SHA administrativo pós-merge certificado
[x] CI push de main revalidada no SHA exato, com 11/11 checks verdes
[x] problema reproduzido com sucesso sintético e arquivo vazio
[x] dependências F3.4/F3.6/F3.5/F3.C2 revalidadas
[x] contrato local/MCP/registry e fronteira de integração congelados
[x] escopo permitido e proibido congelado arquivo por arquivo
[x] aceite positivo, negativo, transporte, regressão, quality, pacote e scope congelado
[x] rollback executável e não destrutivo congelado
[x] executor, runtime, horário, autorização e checkpoint definidos
[x] autorização cobre branch/checkpoint/implementação local, não push/PR/merge/F3.7
```

Nenhum arquivo de implementação F3.8 foi alterado antes deste dossiê `READY`. O primeiro commit da
branch recebe a tag local `checkpoint/f3.8-ready`; somente depois desse checkpoint a implementação
congelada pode começar.

## Evidências de execução

### Checkpoint e implementação

| Evidência | Resultado observado |
|---|---|
| Freeze anterior ao código | commit `b0215b2853cbd9440ef13ff1ef5be9c36db81ba0`; tag local `checkpoint/f3.8-ready` |
| Implementação | commit local `45d3b059db071bdb98285e2ad821f525f80a9de6` |
| Dependência | `mcp>=1.26,<2` declarada; lock resolveu `mcp==1.29.0` e 54 pacotes no total |
| Efeito remoto | nenhum push, PR, merge, tag remota, instalação live ou mudança de proteção |

O adapter legado foi reproduzido antes do freeze: retornava `True`, criava um arquivo ausente vazio e
não realizava transporte, handshake, discovery ou edição. Depois do checkpoint, ele foi substituído
por transporte MCP oficial e efeitos reais verificados; o fallback local é uma seleção explícita do
chamador, nunca consequência silenciosa de falha Serena.

### Evidência negativa e recertificação

1. As primeiras execuções locais de pytest não obtiveram o diretório temporário global do sandbox.
   Um `--basetemp` confinado foi usado somente para iteração; os comandos congelados com o `uv`
   versionado foram repetidos depois fora dessa restrição e ficaram verdes.
2. A prova local revelou diferenças CRLF no Windows. O adapter passou a preservar LF/CRLF de forma
   explícita e as fixtures gravam bytes determinísticos; os 15 casos foram recertificados.
3. O timeout stdio do MCP chegou como `ExceptionGroup` durante o unwind assíncrono. A deadline do
   `asyncio.timeout` tornou-se a fonte de verdade e o erro voltou a `SerenaConnectionError` tipado;
   stdio, Streamable HTTP e todos os negativos foram repetidos.
4. A primeira regressão documental falhou em uma asserção que atravessava quebra de linha. O claim
   negativo já existia; o teste passou a validar sua semântica e o gate completo foi repetido.
5. As primeiras chamadas diretas ao `uv.exe` foram negadas pelo sandbox. O mesmo binário versionado
   foi então executado com autorização do runtime e todos os critérios congelados foram repetidos.
6. O primeiro smoke do wheel falhou com `FileNotFoundError` porque seu subprocesso procura `uv` pelo
   `PATH`. O diretório do mesmo `uv` versionado foi exposto somente ao processo do smoke; a segunda
   execução instalou a wheel isoladamente e ficou verde.
7. `ruff check .` retornou exit 0 com avisos ao tentar ler caches protegidos. A cobertura autoritativa
   foi repetida sobre `src`, `compiler` e `tests`, sem avisos e com `All checks passed!`.
8. A limpeza ampla de `.harness` foi corretamente recusada por conter estado auditável preexistente.
   A inspeção provou que somente `.harness/test-tmp` nasceu neste turno; apenas esse subdiretório foi
   removido, enquanto `.harness/state` de 3 de agosto permaneceu intocado e ignorado pelo Git.

Nenhuma falha foi convertida em sucesso. Cada critério afetado foi repetido depois do reparo; nenhuma
asserção, limite, fronteira ou teste congelado foi removido ou enfraquecido.

### Matriz final do aceite

| Critério | Resultado final |
|---|---|
| Edição local | `15 passed` |
| Serena MCP | `10 passed, 1 skipped`; o skip é exclusivamente `SERENA_MCP_COMMAND` live opt-in |
| Registry/tool loop | `27 passed`; oito schemas/handlers, deny-wins e backend ausente fail-closed |
| Compatibilidade | `52 passed` para path guard, worktree, terminal e imports públicos |
| Documentação | `20 passed, 6 subtests passed` |
| Regressão integral | `623 passed, 2 skipped, 6 subtests passed`; skips live Serena e OpenAI opt-in |
| Tipagem | mypy Windows e Linux: `Success`, 102 arquivos em cada plataforma |
| Qualidade | Ruff em `src compiler tests`: `All checks passed!`; compileall exit 0 |
| Lock | `uv lock --check`: 54 pacotes resolvidos, exit 0 |
| Pacote | sdist e wheel `0.1.0` construídas; smoke isolado importou a wheel e validou CLI/metadata/origem |
| Whitespace | `git diff --check` exit 0 |
| Escopo | somente a allowlist F3.8; fronteiras proibidas byte-idênticas a `checkpoint/f3.8-ready` |

### Fronteira entregue

- `LocalEditingAdapter` aplica leitura, listagem, busca literal e patch único/atômico sob `PathGuard`;
- `SerenaAdapter` exige configuração stdio ou Streamable HTTP, initialize/discovery, raiz exata,
  capability allowlisted e mudança real de digest;
- `build_operational_tool_router` registra até oito tools somente com adapters explicitamente
  injetados; ausência de terminal/Serena permanece `ToolUnavailableError` e policy deny-wins prevalece;
- Git oferecido pela F3.8 é somente `status`/`diff` por argv fixo; nenhuma operação mutável foi criada;
- defaults, lifecycle, CLI, policy, graph, providers, promoção, F3.7 e instalação live permanecem fora.

```text
[x] problema sintético removido e negativos fail-closed comprovados
[x] checkpoint anterior à implementação preservado
[x] todos os critérios congelados executados e verdes depois dos reparos
[x] suíte integral, quality, lock, pacote, smoke e escopo aprovados
[x] documentação reconciliada com o estado real e sem claim de integração automática/live
[x] somente temporários criados no turno removidos; estado preexistente preservado
[x] nenhum push, PR, merge, tag remota, F3.7 ou configuração externa antecipados
```

A F3.8 alcançou inicialmente `COMPLETED_LOCAL / PROMOTION_PENDING`. O commit documental desse primeiro
fechamento recebeu a tag local histórica `checkpoint/f3.8-complete`. A branch foi publicada sem tags e
o PR único #29 foi aberto após autorização nominal; o merge permanece uma fronteira separada.

## R1 — CI do PR #29 reabre portabilidade e promoção

> **Observado em:** `2026-08-08 23:18 -03:00`
> **Run:** `31289781573`, evento `pull_request`, head `33688e6c49574512e27c3ae2c51928ce48ab6815`
> **Estado:** `REPAIR_ACTIVE / PROMOTION_BLOCKED`

A branch foi publicada sem tags e o PR único [#29](https://github.com/Wf-ops1/Harnessinfra/pull/29)
foi aberto para `main`. O GitHub comprovou 3 commits, base/head corretos e iniciou 11 checks. O
resultado terminal invalida o fechamento positivo anterior:

| Check | Resultado observado |
|---|---|
| Package Ubuntu/Windows 3.12 | 2 sucessos |
| Quality Ubuntu/Windows 3.11/3.14 | 4 sucessos |
| Tests Windows 3.14 | sucesso |
| Tests Ubuntu 3.11/3.14 | falha de transporte do servidor MCP de teste |
| Tests Windows 3.11 | falha |
| `CI required` | falha; merge bloqueado |

Nos dois jobs Ubuntu, a suíte registrou `10 failed, 600 passed, 1 skipped, 6 subtests passed`. O
processo MCP encerrou durante `initialize`; stdio resultou em `SerenaConnectionError: ...
ExceptionGroup` e o servidor Streamable HTTP saiu antes de aceitar conexões. O job Ubuntu 3.14 está
em `93184884320`; Ubuntu 3.11 em `93184884322`.

A reprodução focada isolada em Windows/Python 3.11 passou (`17 passed, 1 skipped`), provando que o
contrato isolado não basta. A suíte unitária completa nessa mesma versão revelou outra divergência:
`test_list_files_is_bounded_sorted_and_does_not_recurse_symlink_dirs` percorreu um junction como
diretório, terminando em `609 passed, 1 failed, 1 skipped, 6 subtests passed`. `Path.is_junction()`
não existe no Python 3.11, portanto o teste congelado expôs uma lacuna real de portabilidade.

Os checkpoints `checkpoint/f3.8-ready` e `checkpoint/f3.8-complete` permanecem imutáveis como fatos
históricos; a tag de completion não prova o estado corrente. Nenhum critério será removido. O reparo
permanece restrito aos adapters/fixtures/testes/documentos já permitidos, deve explicar a saída do
servidor MCP no Linux, corrigir junction no Python 3.11 e repetir integralmente o aceite e a CI remota
antes de restaurar `COMPLETED_LOCAL / PROMOTION_PENDING`.

### Recertificação do reparo R1

> **Observado em:** `2026-08-08`
> **Commits:** `4505cf4` (reabertura documental) e
> `3576a0495fd0d02a4413a131ec9848bfd24652ea` (reparo técnico)
> **Run técnico:** `31290430138`, evento `pull_request`, 11/11 checks
> **Estado restaurado:** `COMPLETED_LOCAL / PROMOTION_PENDING`

A investigação comprovou duas causas independentes. Nos testes Linux, `Path.resolve()` removia a
identidade do executável da virtualenv e iniciava o Python base, sem o pacote `mcp`; a configuração
agora preserva o caminho absoluto fornecido e revalida essa identidade antes de cada sessão. No
Windows/Python 3.11, onde `Path.is_junction()` não existe, o adapter agora reconhece reparse points
por atributos do filesystem e não atravessa junctions.

Depois do reparo, o aceite focado terminou em `32 passed, 1 skipped`; a suíte isolada em Python 3.11,
em `610 passed, 1 skipped, 6 subtests passed`; e a regressão integral, em
`623 passed, 2 skipped, 6 subtests passed`. Mypy Windows/Linux passou nos 102 arquivos, Ruff,
compileall, lock, build, smoke do wheel, whitespace, escopo e fronteiras proibidas ficaram verdes.
Nenhuma asserção, plataforma, limite ou negativo foi removido ou enfraquecido.

O run `31290430138` recertificou o head técnico exato `3576a04` com 11/11 checks, inclusive
`CI required`, e sem conflitos com `main`. O primeiro run vermelho permanece como evidência histórica,
não como estado corrente. Esta reconciliação documental deve receber a mesma CI completa no seu head
antes da pausa para a autorização nominal de merge. Checkpoints e tags continuam imóveis e locais;
nenhum merge, tag remota, F3.7 ou configuração Serena live foi executado.

## R2 — Recongelamento do alinhamento documental pré-merge

> **Observado em:** `2026-08-08`
> **Autorização:** corrigir e recertificar o alinhamento documental pré-merge no PR #29
> **Baseline técnico:** head `b898d88fd39498c23b49dac48acb4c653e7d045c`, run
> `31290644133`, 11/11 checks
> **Estado:** `REPAIR_ACTIVE / PROMOTION_BLOCKED`

A revisão solicitada depois da CI verde encontrou divergências correntes que a certificação R1 não
capturou. O `README.md` ainda dizia que a branch aguardava publicação; o `TASK.md` remetia a promoção
ao primeiro commit do gate seguinte, regra histórica da DEC-011 substituída pela reconciliação
administrativa imediata da DEC-014. Além disso, documentos públicos de estado ainda confundiam a
ausência de integração automática no lifecycle com ausência das primitivas já entregues: providers
reais/configuráveis, worktree Git externo, terminal por `argv`, edição confinada e Serena MCP explícito.

Essa evidência negativa não invalida os checks técnicos anteriores, mas invalida o claim de alinhamento
documental e bloqueia o merge. As descrições devem preservar simultaneamente dois fatos: as primitivas
são reais e testadas; o produto ainda não as compõe automaticamente no lifecycle, e promoção,
Codebase-Memory, doctor, rollback e E2E operacional permanecem incompletos.

### Ampliação documental congelada R2

Somente para este reparo, a allowlist documental passa a incluir também:

- `docs/agentic_lifecycle_audit.md`;
- `docs/audit_report.md`, somente para classificá-lo explicitamente como snapshot histórico;
- `docs/user_guide.md`;
- `docs/walkthrough.md`;
- `docs/walkthrough_audit.md`;
- regressões semânticas nos testes documentais já permitidos.

Continuam proibidos `src/`, dependências, schemas, defaults, lifecycle, CLI, workflows de CI e qualquer
efeito F4/F3.7. O checkpoint local `checkpoint/f3.8-r2-docs-ready` deve apontar para o commit deste
recongelamento antes da primeira correção nesses quatro documentos. A recertificação exige diff
exclusivamente documental/testes, encoding e links íntegros, suíte documental completa, Ruff no teste
alterado, `git diff --check` e CI remota 11/11 no head final do PR #29. Nenhuma tag será publicada.

A busca semântica posterior ao checkpoint encontrou `docs/audit_report.md`, relatório datado da F0.5,
fora de `docs/tasks/completed/` e ainda formulado como estado atual. Ele entra na allowlist somente para
receber uma advertência explícita de snapshot histórico e links para as fontes correntes; seus achados
originais não serão apagados nem reclassificados retroativamente.

### Recertificação do reparo R2

> **Checkpoint:** `checkpoint/f3.8-r2-docs-ready` em
> `258a47be6e8c6e3ebaeda2c23b77d85b3b128a7f`
> **Estado restaurado:** `COMPLETED_LOCAL / PROMOTION_PENDING`
> **Fronteira remota:** o novo head ainda deve receber 11/11 checks antes de qualquer merge

O reparo atualizou somente README, painel, documentos públicos de estado, este dossiê e regressões
documentais. As fontes agora distinguem consistentemente as primitivas reais — providers HTTP,
GraphExecutor/lifecycle retomável, worktree Git, terminal `argv`, edição confinada, Serena MCP e
registry opt-in — da composição automática ainda ausente. O relatório F0.5 foi preservado como
snapshot histórico, sem reescrever seus achados como se fossem correntes.

O gate documental terminou em `21 passed, 6 subtests passed`; Ruff dos testes documentais e
`git diff --check` ficaram verdes. Mypy Windows e Linux retornou `Success` nos 102 arquivos, Ruff em
`src compiler tests` passou e compileall terminou sem erro. O diff desde o checkpoint R2 contém apenas
documentos e `tests/unit/test_documentation.py`; `src`, `compiler`, `pyproject.toml`, `uv.lock` e
`.github` permaneceram byte-idênticos.

A primeira tentativa integral expirou em 120 segundos após erros de temporário global do sandbox e
não foi usada como evidência. A repetição com `basetemp` confinado concluiu em
`623 passed, 1 failed, 2 skipped, 6 subtests passed`; a única falha foi `PermissionError` ao acessar o
diretório intencional de worktrees no `AppData`. O caso exato foi repetido com acesso normal e passou
(`1 passed`). Uma tentativa adicional de obter um único run externo expirou em 300 segundos sem
resultado terminal e também foi descartada. Nenhum teste ou código foi alterado para contornar o
ambiente; os três temporários R2 foram removidos e o processo Git órfão encerrou antes da limpeza final.

Falha histórica, diagnóstico e tentativas inválidas permanecem explícitos. A autoridade final será a
CI completa Windows/Linux do novo head do PR #29. Merge, tags remotas, Serena live, F4.1 e F3.7
continuam proibidos até as respectivas fronteiras e autorizações.

## R3 — reconciliação administrativa pós-merge

> **Autorizado em:** `2026-08-09T02:00:28-03:00`
> **Autoridade:** o usuário realizou manualmente o merge do PR #29 e autorizou ajustar e alinhar o
> estado documental conforme a DEC-014.

### Problema adicional comprovado

O PR #29 foi mesclado em `main` no merge commit
`e6b5b84bbe8299f8e04b9ad28c0ca0a86269c98f`, mas o painel, este dossiê, o índice e o README ainda
descrevem o merge como pendente. O head final `f941c89fd0ec112aca82621ab9e11244f05962aa`
recebeu 11/11 checks verdes no run de `pull_request` `31292195340`; o merge ocorreu em
`2026-08-09T01:55:00-03:00` e o run de `push` `31295594376` concluiu 11/11 checks verdes no SHA exato
de `main`. A `main` local foi sincronizada por fast-forward e comprovada igual a `origin/main` antes
desta branch documental.

### Escopo recongelado

Permitido:

- certificar a F3.8 como `PROMOTED` e mover seu dossiê de `active/` para `completed/`;
- atualizar `TASK.md`, `README.md`, `docs/tasks/README.md` e regressões documentais/ledger afetadas;
- registrar somente fatos observados do PR #29, merge `e6b5b84` e runs `31292195340`/`31295594376`.

Proibido:

- alterar `src/`, `compiler/`, `.github/`, dependências, lockfile, schemas, defaults ou CI;
- iniciar, congelar ou implementar F4.1; F3.7 permanece depois da F4.7;
- publicar branch, abrir/mesclar PR administrativo, excluir refs ou criar/publicar tags sem
  autorização própria;
- apagar ou reclassificar as evidências negativas R1/R2; esta certificação deve permanecer append-only.

### Aceite e rollback recongelados

```yaml
acceptance:
  documentation: >-
    .\build\f0.6-tools\uv\bin\uv.exe run python -m pytest
    tests/unit/test_task_ledger.py tests/unit/test_documentation.py tests/unit/test_encoding.py -q
  quality: "Ruff nos testes alterados e git diff --check verdes"
  links: "links Markdown relativos resolvem e encoding UTF-8 permanece íntegro"
  scope: >-
    diff restrito ao dossiê F3.8, TASK.md, README.md, índice de tarefas e testes documentais/ledger;
    fronteiras de produto, dependências, schemas, defaults e CI byte-idênticas a e6b5b84
  state: >-
    F3.8 PROMOTED/arquivada; nenhum dossiê ou gate de implementação ativo; F4.1 somente planejada e
    sujeita a nova autorização nominal depois do PR administrativo e de sua CI pós-merge verde
rollback:
  trigger: "qualquer teste, integridade, escopo ou evidência remota falhar"
  procedure: "reverter somente os commits desta reconciliação com git revert; nunca reset destrutivo"
  verify: "repetir suíte documental, Ruff, git diff --check e auditoria de escopo"
```

Este registro deve ser commitado e receber o checkpoint local
`checkpoint/f3.8-promotion-sync-ready` antes das demais edições transversais.

## Certificação final de promoção

| Evidência | Resultado comprovado |
|---|---|
| Head final do PR | `f941c89fd0ec112aca82621ab9e11244f05962aa` |
| CI final do PR | run `31292195340`, evento `pull_request`, 11/11 verde com `CI required` |
| PR | #29, oito commits mesclados sem conflitos por merge commit |
| Merge em `main` | `e6b5b84bbe8299f8e04b9ad28c0ca0a86269c98f`, observado em `2026-08-09T01:55:00-03:00` |
| CI pós-merge | run `31295594376`, evento `push`, SHA exato `e6b5b84`, 11/11 verde com `CI required` |
| Sincronização observada | `main == origin/main == e6b5b84`; workspace limpo antes da branch documental |
| Refs | branch remota de implementação preservada; checkpoints somente locais; nenhuma tag remota F3.8 |

A F3.8 está `PROMOTED`. Leitura, listagem, busca e patch confinados, Git somente leitura, adaptação do
terminal seguro e cliente Serena MCP explícito foram incorporados e validados em `main`. O registry
permanece opt-in e injetável; lifecycle/defaults, configuração live, fallback automático e promoção
F3.7 não foram implicitamente habilitados.

O checkpoint `checkpoint/f3.8-promotion-sync-ready` aponta para
`3c9aa9709a5565b83fe62d2bda99113fa133d4ca`, anterior às demais edições desta reconciliação. O PR
administrativo desta branch certifica somente a promoção da F3.8 e não autoriza iniciar a F4.1.

## Resultado local da reconciliação R3

- o dossiê foi movido para `docs/tasks/completed/F3.8.md`, e painel, README e ledger passaram a
  registrar PR #29, merge `e6b5b84` e CI pós-merge `31295594376` como fatos observados;
- as duas primeiras execuções documentais expuseram uma regressão de teste: a evidência negativa R1
  ainda era procurada no painel curto e depois por uma frase literal inexistente. A asserção passou a
  validar no dossiê arquivado o título R1 e o texto semântico preservado; nenhuma evidência foi
  removida, reescrita ou enfraquecida;
- a repetição final de documentação/ledger/encoding concluiu em `21 passed, 6 subtests passed`;
- Ruff nos dois testes alterados retornou `All checks passed!`, e `git diff --check` ficou verde;
- a auditoria de escopo encontrou somente README, painel, índice, movimento do dossiê e os dois testes
  documentais; `src/`, `compiler/`, `.github/`, `pyproject.toml`, `uv.lock`, contratos, schemas e
  defaults permaneceram byte-idênticos a `e6b5b84`;
- a branch administrativa permanece somente local. Push, abertura/merge de PR, exclusão de branch e
  tags remotas não foram executados; F4.1 continua sem gate e sem implementação ativa.
