# F6.7 — Corrigir knowledge transaction

> **Gate:** `COMPLETED_LOCAL`
> **Lifecycle:** `COMPLETED_LOCAL / PROMOTION_PENDING`
> **Branch:** `task/f6.7-knowledge-transaction`
> **Baseline Git:** `1327f299c2a748fdb3efb759291b67b39bd2598b` (`main == origin/main`)
> **Checkpoint READY:** `checkpoint/f6.7-ready` → `e01d49d6b11b2a27585669280f153f1b474af0c2`, somente local
> **Produto:** `3fd5565d2308eecb667d9782f81b17be74040bd6`
> **Checkpoint completo:** `checkpoint/f6.7-complete` — aponta para o commit documental de certificação, somente local
> **Executor:** `Codex`, único escritor autorizado
> **Autorizado em:** `2026-08-16T19:02:54-03:00`
> **Certificado localmente em:** `2026-08-16T19:38:59-03:00`
> **Runtime:** `C:\Users\walla\OneDrive\Desktop\ai-engineering-harness\.venv\Scripts\python.exe` — Python 3.12.13

## Dependência terminal comprovada

A F6.7 nasce somente depois do encerramento terminal da F6.6. O produto foi incorporado pelo
[PR #79](https://github.com/Wf-ops1/xXHarnessinfraXx/pull/79) no merge
`8be678946dc57244974caf5b485c33425a7466c3`; a CI pós-merge `31963338576` passou 11/11.

A reconciliação administrativa [PR #80](https://github.com/Wf-ops1/xXHarnessinfraXx/pull/80)
encerrou no head `43cb6ea48a2ee0148a9c9d63ec545d6d3e927ee5`, com 11/11 no run `31967664405`,
foi incorporada pelo merge `1327f299c2a748fdb3efb759291b67b39bd2598b` e recebeu 11/11 na CI
pós-merge `31968035375` no mesmo SHA. Pela DEC-014, essa evidência externa complementa o snapshot
histórico do próprio PR administrativo sem criar reconciliação recursiva.

## Problema comprovado

`KnowledgeTransactionManager.recover_if_needed()` lê somente a última linha do journal. Se ela é
`PREPARED`, acrescenta `COMMITTED` sem verificar staging, conteúdo, pointer swap, commit Git, digest,
lock ou concorrência. A reprodução congelada executou:

```text
.\.venv\Scripts\python.exe -m pytest -p no:cacheprovider --basetemp=<externo> tests/unit/test_phase7.py::test_f66_freezes_false_knowledge_recovery_for_f67 -q
1 passed in 1.17s
```

O teste comprova o defeito, não sucesso: `outcome=RECOVERED_tx-missing`, journal
`PREPARED → COMMITTED`, `current.json` ausente e staging ausente. A suíte baseline de
`tests/unit/test_phase7.py` passou `7 passed in 1.03s`, preservando as demais autoridades.

## Baseline conhecido

- `.git` existe; `main == origin/main == 1327f299c2a748fdb3efb759291b67b39bd2598b` estava limpa.
- Branch exclusiva `task/f6.7-knowledge-transaction`, sem upstream, criada desse SHA.
- CI autoritativa da base: `31968035375`, evento `push`, `completed/success`, 10 jobs da matriz mais
  `CI required`, no SHA exato da `main`.
- Nenhum dossiê de implementação estava ativo; nenhum stash ou worktree adicional existia.
- Runtime congelado: `.venv\Scripts\python.exe`, Python 3.12.13.

## Contrato técnico congelado

1. `execute_transaction(tx_id, new_ki, *, commit_sha)` exige identificador seguro, KI JSON finita
   com `id` canônico e SHA completo que exista como commit no repositório Git.
2. O índice persistido é JSON canônico content-addressed; `index_digest` é SHA-256 recalculado na
   execução, no recovery e ao carregar o ponteiro corrente.
3. Um lock cross-processo global serializa qualquer alteração knowledge. Cada aquisição avança um
   fencing token durável; `PREPARED`, pointer e resultado terminal registram o token observado.
4. A ordem write-ahead é: staging íntegro → `PREPARED` atômico → snapshot imutável → pointer swap
   atômico condicionado ao predecessor → `COMMITTED` atômico. Nenhuma etapa posterior pode ser
   inferida apenas pela intenção anterior.
5. Recovery aceita somente um `PREPARED` não resolvido. Ele valida journal, SHA Git, digest, staging
   ou snapshot e pointer atual. Pode concluir a mesma transação, abortá-la antes do pointer quando a
   falta/divergência é comprovada, ou falhar fechado se o efeito corrente é ambíguo.
6. Pointer já publicado e idêntico permite somente completar o journal; pointer avançado por outra
   transação nunca é sobrescrito. Transação `COMMITTED` repetida é idempotente somente para o mesmo
   SHA e a mesma KI.
7. Cada novo snapshot incorpora todos os KIs do snapshot corrente e substitui apenas o mesmo `id`;
   atualizar `current.json` não pode perder KIs anteriores.
8. Pointer legado pode ser migrado somente se seu staging antigo existir e validar. Journal legado
   `PREPARED` sem metadados/staging é marcado `ABORTED`, nunca `COMMITTED`.
9. Retenção é explícita, limitada a snapshots comprovadamente `COMMITTED`, preserva sempre o atual e
   nunca remove staging abortado/corrompido ou evidência ambígua.
10. Erros de configuração, integridade, conflito, recovery e escrita são tipados. Nenhum retorno
    sintético, fallback permissivo, cleanup destrutivo ou `shell=True` é permitido.

## Escopo congelado

### Produto permitido

- `src/ai_engineering_harness/knowledge/transaction.py` — protocolo, validação, recovery e retenção;
- `src/ai_engineering_harness/knowledge/synchronizer.py` — propagar o commit SHA obrigatório;
- `src/ai_engineering_harness/knowledge/__init__.py` — exportar erros/resultado públicos necessários.

### Testes e documentação permitidos

- `tests/unit/test_knowledge_transaction.py` — matriz F6.7 nova;
- `tests/unit/test_phase7.py` — substituir a caracterização negativa pelo comportamento seguro;
- `docs/user_guide.md`, `README.md`, `TASK.md`, `docs/tasks/README.md`, este dossiê,
  `tests/unit/test_documentation.py` e `tests/unit/test_task_ledger.py` — contrato e estado.

### Reuso obrigatório sem edição

- `CrossProcessLockManager`, `ExecutionLock`, JSON/digest canônicos e os erros de persistence;
- Git por argv limitado e `shell=False`;
- journal/evidence de execução, lifecycle, worktree, promoção e rollback já promovidos.

### Fora de escopo

- integrar automaticamente knowledge ao lifecycle principal ou alterar o grafo/defaults;
- alterar contratos persistidos de execução, eventos, evidence, approval ou worktree;
- novo backend knowledge, Codebase-Memory, índice semântico, daemon, API, CLI ou workflow;
- dependências, lockfile, package version, CI, provider, tool, gates, promoção ou rollback;
- skip/xfail, relaxamento de teste, cleanup automático de evidência ambígua, push, PR, merge,
  force/bypass, tag remota ou remoção de refs.

## Critérios de aceite congelados

1. Sucesso prova staging, `PREPARED`, snapshot, pointer e `COMMITTED` com SHA existente, digest
   recalculável e fencing monotônico.
2. Duas transações preservam os KIs anteriores; atualizar o mesmo ID substitui somente esse item.
3. Fault injection antes/depois de `PREPARED`, publicação do snapshot, pointer swap e append terminal
   prova recovery idempotente sem segundo pointer, perda de KI ou falso commit.
4. Staging ausente/corrompido antes do pointer resulta `ABORTED`; pointer publicado com snapshot
   ausente/corrompido ou identidade divergente falha fechado e preserva bytes.
5. Concorrência cross-processo é serializada; fencing cresce e token obsoleto não publica pointer.
6. SHA inválido/inexistente, digest adulterado, journal inválido, tx reutilizada com outro conteúdo e
   pointer divergente falham antes de sobrescrever estado válido.
7. Retenção remove somente snapshots antigos comprovadamente committed, preserva o atual e o snapshot
   corrente continua contendo todos os KIs.
8. A caracterização F6.6 deixa de aceitar `RECOVERED_tx-missing`: o mesmo caso termina abortado ou
   bloqueado, sem `COMMITTED` e sem pointer.
9. Dedicado, suíte F6 aplicável e E2E/evidence relevantes permanecem verdes.
10. Antes de `COMPLETED_LOCAL`: full confinada; Ruff global; mypy em todo `src`; compileall;
    `git diff --check`; build sdist/wheel e smoke oficial offline; diff final restrito ao allowlist.

Comandos de aceite usam o runtime congelado, `-p no:cacheprovider` e `--basetemp` fora do checkout.
Qualquer necessidade de schema externo, dependência, lifecycle/defaults ou cleanup de evidência
ambígua reabre o gate como `BLOCKED` e exige recongelamento.

## Rollback

- O checkpoint `checkpoint/f6.7-ready` preserva o gate antes do primeiro arquivo de produto.
- O produto está no commit isolado `3fd5565d2308eecb667d9782f81b17be74040bd6`, revertível com
  `git revert`; nunca `git reset --hard` ou force.
- Rollback restaura somente os arquivos do allowlist e não apaga `.harness/knowledge`, snapshots,
  staging, journals, locks ou fencing tokens criados em projetos externos.
- Após revert, repetir o teste negativo F6.6, a baseline `7/7`, documentação e `git diff --check`.

## Checklist de liberação

- [x] regras, painel, plano 1.1–1.2, Fase 6, F6.6 e DEC-014 lidos;
- [x] PR #80, head, checks, merge e CI pós-merge comprovados terminalmente;
- [x] baseline Git/workspace/runtime/executor registrados;
- [x] branch exclusiva criada da `main` certificada;
- [x] falso recovery reproduzido e baseline `7/7` verde;
- [x] escopo permitido e proibido congelado;
- [x] rollback não destrutivo congelado;
- [x] contrato e critérios congelados;
- [x] autorização nominal registrada;
- [x] commit documental `e01d49d` e `checkpoint/f6.7-ready` existentes;
- [x] produto e fault injection concluídos no commit `3fd5565`;
- [x] dedicado, focado, E2E, suíte integral e controles de qualidade verdes;
- [x] sdist/wheel atuais e smoke oficial offline verdes;
- [x] estado limitado a `COMPLETED_LOCAL / PROMOTION_PENDING`, sem efeito remoto.

## Resultado implementado

- `execute_transaction(tx_id, new_ki, *, commit_sha)` valida identificadores, JSON finito, SHA Git
  completo e conteúdo antes de qualquer publicação.
- staging, journal canônico, snapshot content-addressed e `current.json` obedecem ao protocolo
  write-ahead congelado; lock global e fencing durável protegem escritores concorrentes.
- recovery diferencia intenção de efeito comprovado: staging ausente/corrompido antes do pointer
  termina `ABORTED`; pointer/snapshot ambíguos falham fechados; pointer já idêntico recebe somente o
  terminal `COMMITTED`, sem nova troca.
- snapshots preservam todos os KIs anteriores e substituem somente o mesmo `id`; retries committed
  exigem a mesma KI e o mesmo SHA.
- pointer legado só migra com staging íntegro; `PREPARED` legado sem prova nunca vira falso commit.
- retenção é explícita e remove somente snapshots antigos comprovadamente committed, mantendo o
  atual e toda evidência de staging ambígua.
- `KnowledgeSynchronizer` resolve e propaga o `HEAD` Git verificado; erros públicos são tipados e
  exportados pelo pacote knowledge.

## Certificação local

| Gate | Resultado observado |
|---|---|
| Dedicado F6.7 | `24 passed in 10.78s` |
| Dedicado + F6.6 + E2E | `25 passed in 17.71s` |
| Focado final | `137 passed in 29.21s` |
| E2E lifecycle | `1 passed in 5.02s` |
| Suíte integral confinada | `1049 passed, 5 skipped, 6 subtests passed in 394.77s` |
| Ruff global | `All checks passed!` |
| mypy | `Success: no issues found in 110 source files` |
| compileall | exit `0` em `src` e `tests` |
| Diff | `git diff --check` verde; diff de produto restrito ao allowlist |
| Build | sdist e wheel `0.1.0` gerados com sucesso pelo backend PEP 517 local |
| Smoke oficial offline | package/metadata/CLI `0.1.0`; origem no cache isolado do `uv`; sem bytecode |

Artefatos certificados: wheel com `378969` bytes e SHA-256
`00CCC13443C89BBF3236CA177956AF2C9F0DAC612D558E6548CF0C166CD343C2`; sdist com `332121` bytes e
SHA-256 `BEBA33BA60A6925BB4974A931F97B943DC63DACCB26BA5B9C8B81AA2F5573C94`.

Cinco skips da full são autoridades já existentes e explícitas: Serena live opt-in, três casos POSIX
inaplicáveis ao Windows e integração live de modelo opt-in. Nenhum teste foi removido, relaxado,
marcado com novo skip ou xfail.

## Ocorrências de ambiente durante a certificação

- A primeira forma da full foi rejeitada antes da coleta por sintaxe inválida de `--basetemp`; a
  invocação corrigida, sem alterar teste ou critério, passou integralmente.
- O runtime `.venv` é gerenciado sem `pip/setuptools`; por isso `python -m build --no-isolation` e o
  build isolado offline falharam antes do backend. O build aprovado reutilizou `build` da `.venv` e
  `setuptools 83.0.0` do runtime empacotado, sem download nem alteração de dependência.
- Artefatos antigos ignorados em `dist` foram movidos para backup externo recuperável antes do build.
  O ACL de leitura da wheel atual foi ampliado somente para permitir que o processo isolado fora do
  sandbox a inspecionasse. Nenhuma dessas ocorrências alterou arquivo versionado ou resultado de
  produto.

## Próxima ação

Criar o commit documental e `checkpoint/f6.7-complete` locais. Depois, parar em
`COMPLETED_LOCAL / PROMOTION_PENDING`: push, PR, merge, tag remota e qualquer início de fase seguinte
exigem autorização específica e nova comprovação da autoridade remota.
