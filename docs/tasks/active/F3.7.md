# F3.7 — Promoção Git segura

> **Gate:** `READY (R2)`
> **Lifecycle:** `REPAIR_ACTIVE / PROMOTION_BLOCKED`
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-12T00:48:45-03:00`
> **Branch:** `task/f3.7-safe-promotion`
> **Baseline:** `9f75e35db38fc6648497c01bd8f81dcdecec8029`
> **Runtime:** `.\.venv\Scripts\python.exe` — Python `3.12.13`
> **Checkpoint READY:** `checkpoint/f3.7-ready` → `f84af68fc11f33ed8522a7b132f4d4e0532d8625`
> **Produto local:** `cb80f8b2d86b9ff38075e6f0068e32b62ba4dbb5`
> **Checkpoint de conclusão inicial:** `checkpoint/f3.7-complete` → `ac1d3e2ad5bda0111d5da7e7569d23318c9d762a`
> **Checkpoint R1 READY:** `checkpoint/f3.7-r1-ready` → `6333217d76e2807a253276b84e22d499f8722f93`
> **Reparo R1:** `d31227694344ea89303bfb6853eb238c4ca6d8f7`
> **Checkpoint R1 de conclusão:** `checkpoint/f3.7-r1-complete` → commit documental desta recertificação
> **Checkpoint R2 READY:** `checkpoint/f3.7-r2-ready` → commit documental deste recongelamento

## Problema comprovado

O caminho atual de promoção não satisfaz o plano nem pode receber autoridade para alterar uma branch
original. `src/ai_engineering_harness/runtime/promotion_manager.py`:

- retorna `sha-promoted-<execution_id>` no dry-run e registra `WORKFLOW_PROMOTED` sem commit real;
- executa `git add .` diretamente em `project_root` no caminho live;
- ignora falha de `git commit` e aceita o HEAD existente como se fosse promoção;
- converte erro de Git/OS em `sha-fallback-<execution_id>`;
- não recebe `ProvisionedWorktree`, `base_commit_sha`, branch original, aprovação ou a suíte F4.7;
- não persiste `candidate_commit_sha`/`promotion_commit_sha` no record nem transiciona pelo FSM.

Reprodução final, executada no baseline sem alterar o checkout:

```text
.\.venv\Scripts\python.exe -c "... PromotionManager(...).promote(str(37), dry_run=bool(1)) ..."
sha-promoted-37

Select-String ... -Pattern 'synthetic_sha','["git", "add", "."]','sha-fallback'
linhas 20, 29 e 47 confirmam as três violações
```

Duas tentativas anteriores do comando em linha falharam no parser/quoting antes de importar ou
executar o produto. Não foram tratadas como sucesso; `git status` permaneceu limpo. A terceira
invocação removeu literais problemáticos e materializou a saída sintética acima.

## Baseline conhecido

| Item | Evidência observada |
|---|---|
| Git | `main == origin/main == 9f75e35db38fc6648497c01bd8f81dcdecec8029`, limpa antes da branch |
| Branch exclusiva | `task/f3.7-safe-promotion`, criada do baseline e sem upstream |
| Fechamento F4.8 | PR #49 / merge `72f89e3` / pós-merge `31551685950`, 11/11 |
| Reconciliação F4.8 | PR #50, head `a15b918`, run `31554671587` 11/11; merge `9f75e35`; pós-merge `31557794240` 11/11 |
| Dependência F3.6 | `ExternalWorktreeManager` e referência durável promovidos; branch/worktree ligados ao base SHA |
| Dependência F4.7/F4.8 | suíte canônica commit-bound, guard de conclusão e repair loop promovidos |
| Record/FSM | campos `candidate_commit_sha`, `promotion_commit_sha`, estados `PROMOTING`, `BLOCKED_BASE_CHANGED` e `DRY_RUN_COMPLETED` já existem |
| Dossiês ativos anteriores | zero |
| Runtime | `.\.venv\Scripts\python.exe`, Python `3.12.13` |
| Workspace | nenhuma mudança preexistente; somente este gate documental será criado antes do produto |
| Publicação | branch local; nenhum push, PR, merge, tag remota ou exclusão de ref F3.7 |

## Escopo congelado

### Contrato operacional

1. `ExternalWorktreeManager` continua dono da identidade Git F3.6 e passa a criar um commit candidato
   somente no worktree externo validado, atualizar sua referência durável e devolver o SHA Git real.
2. O candidate commit deve conter exatamente as mudanças presentes no worktree autorizado; nenhuma
   operação de stage/commit ocorre no checkout original.
3. `ExecutionLifecycleService` é o owner da composição. Com promoção explicitamente injetada, persiste
   o candidate SHA no `ExecutionRecord`, mantém o fluxo em `VERIFYING` após a suíte integral e somente
   aceita promoção quando a última suíte canônica F4.7 passou no mesmo candidate SHA.
4. Promoção live exige `ApprovalStatus.APPROVED`, branch original ainda selecionada, checkout original
   limpo e `HEAD == base_commit_sha`. A operação permitida é exclusivamente
   `git cherry-pick <candidate_sha>` por argv, `shell=False`.
5. Divergência de branch ou base impede qualquer cherry-pick, persiste/transiciona para
   `BLOCKED_BASE_CHANGED` e devolve erro tipado com instrução de restart/rebase.
6. Sucesso captura o novo SHA real do checkout original, persiste `promotion_commit_sha`, registra
   evidência durável e só então permite `COMPLETED`.
7. Dry-run cria e certifica o candidate commit, não altera o checkout original, não define
   `promotion_commit_sha`, não registra promoção real e termina em `DRY_RUN_COMPLETED`.
8. Falha de comando, saída inválida, candidate ausente/divergente, gates incompletos/reprovados ou
   aprovação ausente falham fechados; nenhum SHA sintético/fallback é permitido.
9. Efeito Git iniciado deve possuir write-ahead durável. Outcome ausente não autoriza repetir
   `cherry-pick` cegamente nem declarar sucesso; recuperação precisa provar o SHA real ou bloquear.
10. Sem manager/provider F3.7 explicitamente injetado, o comportamento promovido F4.7 permanece
    compatível e conclui após a suíte integral. Não há composição automática de CLI/defaults neste gate.

### Arquivos e áreas permitidos

- `src/ai_engineering_harness/runtime/promotion_manager.py`;
- `src/ai_engineering_harness/runtime/execution_lifecycle.py`;
- `src/ai_engineering_harness/runtime/engine.py` e `runtime/__init__.py`;
- `src/ai_engineering_harness/runtime/state_machine.py`, somente se uma aresta fechada comprovadamente
  ausente impedir o contrato acima;
- `src/ai_engineering_harness/workspace/git_worktree.py` e `workspace/__init__.py`;
- testes focados em `tests/unit/test_promotion_manager.py`, `test_git_worktree.py`,
  `test_verification_lifecycle.py`, `test_execution_lifecycle.py`, `test_state_machine.py`;
- E2E novo `tests/e2e/test_safe_promotion.py` e ajuste estritamente necessário em
  `tests/e2e/test_full_lifecycle.py`;
- painel, este dossiê, índice, README, guia/auditoria/walkthrough e testes documentais afetados.

### Fora de escopo

- push, merge, rebase automático, force-push, tag, remoção de branch/ref ou Git sobre este repositório
  fora de fixtures temporárias dos testes;
- rollback/revert F6, reindexação/knowledge sync, evidence final F7 ou gate integral F7.1;
- schema novo de record/evento/approval, migração incompatível ou mudança de versão/dependência;
- aprovação F5.6 vinculada a diff/candidate, expiração, trust policy, secrets ou budget F5;
- construir automaticamente worktree/provider/tools no CLI/lifecycle padrão;
- `git add .`, shell string, SHA sintético/fallback, merge automático ou alteração silenciosa da
  branch originalmente selecionada;
- enfraquecer o guard F4.7, aceitar `0/0`, suíte targeted sem full final ou resultado de outro commit;
- alterar CI, lockfile, compiler, policies, contracts, defaults ou grafos sem parar e recongelar.

### Descoberta que reabre o gate

Necessidade de arquivo/efeito fora da allowlist, novo schema, transição FSM material, mudança de
contrato público incompatível, alteração de CLI/default ou impossibilidade de provar recuperação após
efeito Git interrompe o produto. A descoberta deve ser registrada, o escopo recongelado e um novo
checkpoint criado antes da retomada.

## Critérios de aceite congelados

### Provas focadas obrigatórias

1. Commit candidato real nasce no worktree externo, contém a alteração esperada, atualiza a referência
   durável e deixa checkout/branch original byte-idênticos.
2. Candidate vazio, worktree sujo após commit, referência adulterada, branch incorreta e SHA inválido
   falham tipados sem SHA alternativo.
3. Suíte reprovada, `0/0`, targeted sem full, resultado de outro SHA ou aprovação diferente de
   `APPROVED` bloqueiam promoção antes do cherry-pick.
4. Live promotion revalida branch, limpeza e base imediatamente antes do efeito, executa somente
   `git cherry-pick <candidate_sha>`, captura SHA novo real e persiste record/eventos.
5. Base avançada termina em `BLOCKED_BASE_CHANGED`; branch original e conteúdo do avanço permanecem
   intactos, sem merge/cherry-pick automático.
6. Dry-run preserva checkout original, não produz `promotion_commit_sha` ou evento promoted e termina
   em `DRY_RUN_COMPLETED` com candidate real.
7. Crash/injeção entre write-ahead, cherry-pick e outcome não duplica efeito nem fabrica sucesso;
   recuperação prova o outcome real ou falha fechada.
8. E2E temporário demonstra alteração real → candidate commit → aprovação canônica → full suite verde
   no candidate → cherry-pick → conteúdo/SHA real no checkout original.
9. Compatibilidade: sem integração F3.7 injetada, os fluxos F4.7/F4.8 promovidos mantêm semântica e
   resultados anteriores.

### Comandos exatos

```text
.\.venv\Scripts\python.exe -m pytest tests\unit\test_promotion_manager.py tests\unit\test_git_worktree.py tests\unit\test_verification_lifecycle.py tests\unit\test_execution_lifecycle.py tests\unit\test_state_machine.py tests\e2e\test_safe_promotion.py tests\e2e\test_full_lifecycle.py -q -p no:cacheprovider --basetemp <temp-exclusivo>
.\.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider --basetemp <temp-exclusivo>
.\.venv\Scripts\python.exe -m mypy src
.\.venv\Scripts\python.exe -m ruff check .
.\.venv\Scripts\python.exe -m compileall -q src tests
.\.venv\Scripts\python.exe -m pytest tests\unit\test_documentation.py tests\unit\test_task_ledger.py tests\unit\test_encoding.py -q -p no:cacheprovider --basetemp <temp-exclusivo>
git diff --check
git diff --name-status <checkpoint-ready>...HEAD
.\.venv\Scripts\python.exe -m build
smoke isolado da wheel em venv temporária externa ao checkout, sem rede e sem bytecode no repo
```

Todos exigem exit code zero. A regressão integral não pode reduzir contagem por remoção/skip de teste.
Build/smoke devem usar os artefatos do commit certificado. O diff final deve permanecer na allowlist;
`pyproject.toml`, `uv.lock`, `.github/`, schemas, contracts, policies e defaults devem estar intactos.

## Implementação e certificação local

> **Observado em:** `2026-08-12T01:46:44-03:00`
> **Estado:** `COMPLETED_LOCAL / PROMOTION_PENDING`
> **Publicação:** não autorizada; branch sem upstream e checkpoints somente locais
> **Commit de produto:** `cb80f8b2d86b9ff38075e6f0068e32b62ba4dbb5`

### Resultado entregue

1. `ExternalWorktreeManager.create_candidate_commit()` cria ou recupera um único commit real cuja
   árvore representa o worktree autorizado e cujo parent é exatamente o base SHA. Stage, `write-tree`,
   `commit-tree` e atualização atômica da branch ocorrem somente no worktree externo.
2. `PromotionManager` não contém SHA sintético/fallback. Dry-run prova ausência de efeito; live executa
   somente `git cherry-pick <candidate_sha>` por argv/`shell=False` e reconcilia parent, árvore,
   autor, data e mensagem antes de aceitar uma retomada como promoção exata.
3. `ExecutionLifecycleService` recebeu composição opt-in: write-ahead/outcome para candidate e
   promoção, CAS recuperável dos SHAs reais, aprovação `APPROVED`, última suíte full no candidate SHA,
   `PROMOTING → COMPLETED`, `BLOCKED_BASE_CHANGED` e `DRY_RUN_COMPLETED`.
4. Com manager F3.7 injetado, uma suíte full verde permanece em `VERIFYING` até promoção. Sem essa
   injeção, o contrato promovido F4.7 continua concluindo após a suíte com semântica compatível.
5. `RuntimeEngine` expõe as duas operações somente por delegação explícita ao lifecycle; CLI/defaults
   não foram ligados automaticamente e permanecem fora do gate.

### Evidência executada

| Gate | Resultado observado |
|---|---|
| Primitivo Git inicial | `29 passed`; Ruff, mypy e compileall verdes |
| Recovery endurecido | `12 passed` em manager + lifecycle; commit independente com mesma árvore é bloqueado |
| Focado congelado ampliado | `74 passed` em manager, lifecycle, worktree, F4.7, FSM e E2E |
| Regressão integral final | `774 passed, 5 skipped, 6 subtests passed` em uma única execução, exit 0 |
| Tipos | mypy: `Success: no issues found in 106 source files` |
| Qualidade | `ruff check .`, `compileall -q src tests` e `git diff --check` verdes |
| Build | sdist e wheel `ai_engineering_harness-0.1.0` construídos pelo PEP 517 |
| Smoke oficial | `tests/ci/smoke_wheel.py`, `UV_OFFLINE=1`, CLI/metadata/package `0.1.0`, origem externa |
| Compatibilidade | testes promovidos F4.7/F4.8 e FSM incluídos no gate focado e na regressão integral |

### Evidência negativa e resolução

- O primeiro pytest focado nem iniciou porque o sandbox negou o temp global; `basetemp` exclusivo
  tornou o gate executável e ele passou. Colocar esse temp dentro do repo contaminou corretamente o
  teste de UTF-8; os artefatos foram removidos e todas as execuções seguintes usaram root externo.
- A primeira regressão integral atingiu timeout em ~99% e mostrou duas falhas sem resumo; não foi
  aceita. A repetição externa isolou uma única falha ambiental em `SandboxProvider` por bloqueio de
  `AppData`; o teste isolado passou com acesso autorizado e a regressão integral repetida sob a mesma
  permissão passou 774/5/6 em uma execução.
- O primeiro build foi bloqueado no temp/rede do sandbox; repetido com acesso autorizado, passou. O
  primeiro smoke externo não encontrou `uv.exe`; uma venv efêmera externa recebeu `uv 0.12.3` do
  cache e o smoke oficial foi então executado offline com sucesso.

Nenhuma ocorrência acima foi promovida como sucesso antes da repetição exit 0. Nenhuma exigiu mudar
CI, dependências, lockfile, policy, schema, contrato ou fonte fora da allowlist congelada.

## Rollback

- checkpoint READY: `checkpoint/f3.7-ready` aponta para `f84af68`, anterior ao produto;
- checkpoint local de conclusão: `checkpoint/f3.7-complete`, apontando para este commit documental;
- antes de publicação, rollback é abandonar somente a branch F3.7 ou aplicar inversão explícita dos
  hunks/`git revert` dos commits próprios; nunca `reset --hard`;
- depois de publicação, usar `git revert` dos commits F3.7 ou do merge completo por autorização
  separada; não force-push;
- fixtures E2E usam repositórios e worktrees temporários e executam cleanup explícito sem `--force`;
- se um teste deixar cherry-pick em andamento numa fixture, abortar somente nessa raiz temporária
  validada e comprovar que o repositório de desenvolvimento não foi tocado;
- após rollback, repetir gate focado, regressão aplicável, Ruff, mypy, diff check e verificar
  `main == origin/main == 9f75e35` quando o retorno integral for necessário.

Gatilhos: mudança fora do escopo, qualquer efeito Git no checkout de desenvolvimento, SHA sintético,
relaxamento de gate/aprovação, estado positivo contradito ou falha que não possa ser recuperada sem
perda de trabalho.

## Checklist de liberação

```text
[x] problema reproduzido com saída sintética e linhas exatas
[x] baseline Git, runtime, upstream e CI pós-F4.8 registrados
[x] dependências F3.6/F4.7/F4.8 comprovadamente promovidas
[x] escopo permitido e proibido congelados
[x] critérios positivos, negativos, E2E, regressão e build congelados
[x] rollback não destrutivo e gatilhos definidos
[x] executor único e autorização nominal registrados
[x] gate documental PREPARING: `27 passed, 6 subtests passed`; Ruff e diff check verdes
[x] gate promovido para READY; commit e checkpoint local são a saída imediata deste documento
[x] nenhuma evidência negativa posterior contradiz READY
[x] candidate real e singular criado somente no worktree externo
[x] promoção live limitada a cherry-pick e sem SHA sintético/fallback
[x] full suite no candidate e aprovação canônica guardam promoção
[x] dry-run, base divergente e recovery entre efeito/outcome comprovados
[x] compatibilidade F4.7/F4.8 sem integração injetada preservada
[x] gate focado 74 e regressão integral 774/5/6 verdes
[x] mypy, Ruff, compileall, diff check, build e smoke oficial verdes
[x] lifecycle local restaurado para COMPLETED_LOCAL / PROMOTION_PENDING
```

## Próxima ação exata

Criar o checkpoint local `checkpoint/f3.7-r2-ready`, reparar as duas assertions E2E remanescentes e
repetir todo o aceite. Mesclar somente se cada job e `CI required` ficarem verdes no novo SHA exato.
Tag remota, exclusão de ref e início de outra tarefa permanecem proibidos.

## Publicação do PR

> **Observado em:** `2026-08-12T02:10:18-03:00`
> **Autorização nominal:** `autorizo` — publicar a branch e abrir o PR F3.7
> **Estado:** `PR_OPEN / CHECKS_PENDING`

| Evidência | Resultado observado |
|---|---|
| Branch remota | `origin/task/f3.7-safe-promotion`, criada sem force-push e configurada como upstream |
| Pull request | [#51](https://github.com/Wf-ops1/Harnessinfra/pull/51), não-draft contra `main` |
| Head na abertura | `ac1d3e2ad5bda0111d5da7e7569d23318c9d762a` |
| Base | `main == origin/main == 9f75e35db38fc6648497c01bd8f81dcdecec8029` |
| Tags | `checkpoint/f3.7-ready` e `checkpoint/f3.7-complete` permanecem somente locais |

Este registro produzirá um novo head documental no mesmo PR. Todos os checks desse head, inclusive
`CI required`, precisam ser observados antes de qualquer pedido de merge. A autorização atual não
inclui merge, tag remota, exclusão de refs ou início de outra tarefa.

## Reparação R1 — igualdade legítima entre candidate e promotion SHA

> **Evidência negativa observada em:** `2026-08-12T02:17:54-03:00`
> **Run:** [31565797052](https://github.com/Wf-ops1/Harnessinfra/actions/runs/31565797052)
> **Head exato:** `2eab6e86b59ab820298c03e83584864d5a1ae44e`
> **Estado:** `REPAIR_ACTIVE / PROMOTION_BLOCKED`
> **Autoridade corrente:** `autorizo` — auditar, reparar e mesclar somente depois de todos os checks verdes

### Evidência negativa e causa comprovada

Os jobs `Tests / ubuntu-latest / Python 3.11` (`94017253149`) e Python 3.14 (`94017253186`)
falharam no mesmo teste, enquanto ambos os jobs Windows de testes e os demais jobs já concluídos
passaram. Em cada Ubuntu, o resultado foi `1 failed, 748 passed, 1 skipped, 6 subtests passed`:

```text
FAILED tests/unit/test_promotion_manager.py::test_live_promotion_cherry_picks_and_recovers_the_exact_real_sha
AssertionError: assert <promotion_commit_sha> != <candidate_commit_sha>
```

O teste assumia que um cherry-pick sempre cria SHA diferente. Isso não faz parte do contrato Git:
quando candidato e promoção têm o mesmo pai, árvore, identidade, mensagem e timestamp de commit no
mesmo segundo, o objeto commit resultante é byte-idêntico e portanto possui o mesmo SHA. Os dois logs
Ubuntu comprovaram exatamente esse caso. A promoção real terminou no conteúdo candidato, com parent
igual ao base, e a recuperação subsequente aceitou o outcome; somente a desigualdade artificial falhou.

### Escopo recongelado do R1

- permitido: `tests/unit/test_promotion_manager.py`, `tests/unit/test_documentation.py`,
  `tests/unit/test_task_ledger.py`, este dossiê e `TASK.md`;
- proibido: qualquer mudança em `src/`, CI, dependências, lockfile, schemas, contracts, policies,
  defaults, grafos ou no contrato público;
- substituir apenas a premissa `promotion_sha != candidate_sha` por prova direta de que houve
  exatamente uma chamada `git cherry-pick <candidate_sha>`;
- preservar e repetir as provas existentes de SHA real, `HEAD`, parent igual ao base, árvore/conteúdo
  candidato e recovery sem segundo efeito;
- igualdade de SHA é aceita somente como resultado real observado do Git, nunca como SHA sintético,
  fallback ou atalho que dispense o cherry-pick.

### Aceite e rollback do R1

O R1 repete todos os comandos exatos da seção **Critérios de aceite congelados**, inclusive gate
focado, regressão integral, mypy, Ruff, compileall, testes documentais, diff/allowlist, build e smoke
isolado da wheel. Além disso, o teste reparado deve passar repetidamente e comprovar uma única chamada
live e nenhuma repetição durante recovery. Qualquer redução de contagem, skip novo ou alteração fora da
allowlist mantém `PROMOTION_BLOCKED`.

O checkpoint local `checkpoint/f3.7-r1-ready` preserva este recongelamento antes do teste. Rollback é
feito por `git revert` dos commits R1 ou inversão explícita de seus hunks, sem force-push ou
`reset --hard`; depois, reexecutar teste focado, documentação, Ruff e `git diff --check`.

A primeira validação documental do R1 falhou corretamente em cinco assertions que ainda exigiam o
snapshot anterior `CERTIFIED / PR_OPEN / CHECKS_PENDING`. Como o checkpoint R1 ainda não existia,
esses dois testes de estado foram adicionados à allowlist antes do congelamento; eles devem agora
exigir `REPAIR_ACTIVE / PROMOTION_BLOCKED`, o run falho e o checkpoint R1, sem tocar produto.

### Recertificação local integral do R1

> **Observado em:** `2026-08-12T02:36:41-03:00`
> **Commit do reparo:** `d31227694344ea89303bfb6853eb238c4ca6d8f7`
> **Estado restaurado:** `COMPLETED_LOCAL / PROMOTION_PENDING`
> **CI remota:** ainda pendente para o novo head; o run falho `31565797052` permanece histórico

| Gate | Resultado observado |
|---|---|
| Repetição da reprodução | teste Git real reparado passou `10/10`; cada execução observou somente um `git cherry-pick <candidate_sha>` |
| Focado congelado | `74 passed` em manager, lifecycle, worktree, F4.7, FSM e E2E |
| Documentação/ledger/encoding | `27 passed, 6 subtests passed` |
| Regressão integral única | `774 passed, 5 skipped, 6 subtests passed` em `344.76s`, exit 0 |
| Tipos e qualidade | mypy em 106 arquivos, Ruff, compileall e `git diff --check` verdes |
| Escopo R1 | `checkpoint/f3.7-r1-ready...d312276`: somente `tests/unit/test_promotion_manager.py`; zero mudança em `src/` |
| Build | sdist e wheel `ai_engineering_harness-0.1.0` reconstruídos no commit R1 |
| Smoke oficial | `UV_OFFLINE=1`; metadata/package/CLI `0.1.0`; import originado de ambiente externo ao checkout |

A sequência agregada do gate focado atingiu o timeout de 180 segundos depois de já registrar
`74 passed` e `27 passed, 6 subtests passed`, antes dos quality gates. Esses resultados não foram
estendidos por inferência: mypy, Ruff, compileall e diff check foram executados separadamente e
passaram. Nenhum critério falho foi removido; a prova direta da operação Git substituiu apenas a
desigualdade de SHA não garantida.

O estado positivo restaurado é exclusivamente local. Merge continua bloqueado até a branch R1 ser
publicada e todos os jobs do PR, inclusive Ubuntu 3.11/3.14 e `CI required`, passarem no head final.

## Reparação R2 — cobertura E2E da mesma premissa inválida

> **Evidência negativa observada em:** `2026-08-12T02:44:52-03:00`
> **Run:** [31567250425](https://github.com/Wf-ops1/Harnessinfra/actions/runs/31567250425)
> **Head exato:** `5c8016252d2fe435fd2582bc49d947842724f726`
> **Estado:** `REPAIR_ACTIVE / PROMOTION_BLOCKED`

### Diagnóstico append-only

O R1 corrigiu a primitiva unitária: ambos os jobs Ubuntu registraram a suíte unitária integral verde
em `749 passed, 1 skipped, 6 subtests passed`. Porém, a suíte E2E separada ainda continha a mesma
premissa em dois pontos de `tests/e2e/test_safe_promotion.py`:

- Python 3.11 (`94021523104`): linhas 302 e 503 falharam; `2 failed, 26 passed, 1 skipped`;
- Python 3.14 (`94021523172`): linha 302 falhou; `1 failed, 27 passed, 1 skipped`;
- `CI required` (`94022258519`) falhou por agregação; os outros oito jobs passaram.

A diferença de contagem entre 3.11 e 3.14 confirma o caráter temporal: numa fixture o cherry-pick
ocorreu no mesmo segundo e preservou o SHA; na outra, um timestamp distinto produziu SHA diferente.
O R1 perde precedência operacional porque sua allowlist cobria apenas o teste unitário, não porque sua
prova esteja incorreta. O produto e a implementação R1 permanecem intactos.

### Escopo e aceite recongelados do R2

- permitido: `tests/e2e/test_safe_promotion.py`, `tests/unit/test_documentation.py`,
  `tests/unit/test_task_ledger.py`, este dossiê e `TASK.md`;
- proibido: qualquer mudança em `src/`, no teste unitário R1, CI, dependências, lockfile, schemas,
  contracts, policies, defaults, grafos ou contrato público;
- instrumentar o `PromotionManager` real nas duas provas E2E e exigir exatamente uma operação mutante
  `git cherry-pick <candidate_sha>` durante promoção e nenhuma repetição;
- preservar as provas de aprovação, suíte full no mesmo candidate SHA, estados do lifecycle, HEAD,
  parent, conteúdo final, SHAs reais persistidos e eventos duráveis;
- igualdade ou diferença entre candidate/promotion SHA não participa mais do resultado esperado.

Antes de restaurar estado positivo, os dois E2E devem passar repetidamente com Git real. Depois, repetir
gate focado, regressão integral única, documentação, mypy, Ruff, compileall, diff/allowlist, build e
smoke offline da wheel. Qualquer falha, skip novo, redução de contagem ou path fora da allowlist mantém
`PROMOTION_BLOCKED`.

O rollback parte de `checkpoint/f3.7-r2-ready` e usa `git revert` ou inversão explícita somente dos
hunks R2, nunca `reset --hard` ou force-push. Checkpoints R1 permanecem imutáveis e somente locais.
