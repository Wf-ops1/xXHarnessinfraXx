# F7.2 — Matriz de testes

> **Gate:** `COMPLETED_LOCAL`
> **Lifecycle:** `PROMOTION_PENDING`
> **Branch:** `task/f7.2-test-matrix`
> **Baseline Git:** `b46ebd9c84cacab6bd58d2fb2712879f6dabc164` (`main == origin/main`)
> **Checkpoint READY:** `checkpoint/f7.2-ready` → `1badf4004b1670774c3fa85c3cae93b6351e49f4`, somente local
> **Implementação:** `bdae858861a9c5294f90a231115b3ed930030117`
> **Checkpoint COMPLETE:** `checkpoint/f7.2-complete` identificará o commit documental desta certificação, somente local
> **Executor:** `Codex`, único escritor autorizado
> **Autorizado em:** `2026-08-17T03:21:29-03:00`
> **Runtime:** `C:\Users\walla\OneDrive\Desktop\ai-engineering-harness\.venv\Scripts\python.exe` — Python 3.12.13

## Dependência terminal comprovada

A F7.1 encerrou antes deste gate. O produto foi incorporado pelo PR #83 no merge
`76f43dd29923c87e00062ca65afd534b5f4f1863` e recebeu 10/10 + `CI required` na CI pós-merge
`31985776520`. A reconciliação administrativa PR #84 abriu no head
`197eb33b0d9c33a87a51cef38b4da39afc5588c6`, certificado por `31998528616`, e encerrou no head
`ceca850083fbbc2a6da54394054b09b6f335c9c7`, certificado por `31999182890`. O merge
`b46ebd9c84cacab6bd58d2fb2712879f6dabc164` recebeu 10/10 + `CI required` no run de `push`
`32000365336`. As branches remotas de produto e documentação foram preservadas e não há tags remotas.

## Problema comprovado

O plano exige uma matriz explícita com 12 camadas e 42 requisitos: contratos, compiler, runtime,
persistence, models, tools, Git, verification, security, observability, E2E e recovery. A suíte atual
possui provas distribuídas para esses comportamentos, porém não existe um artefato canônico que ligue
cada requisito a node IDs pytest reais nem um comando único que valide e execute essa seleção.

Reprodução estrutural em `2026-08-17T03:23:23-03:00`:

```text
rg -n "F7\.2|test_matrix|test matrix|matriz de testes" tests
exit 1; nenhuma ocorrência

Test-Path tests/f7_2_matrix.json
False

.\.venv\Scripts\python.exe -m pytest --collect-only -q -p no:cacheprovider
1055 tests collected in 3.16s
```

Logo, quantidade de testes e CI integral não provam rastreabilidade da F7.2. A lacuna é a ausência de
uma matriz estrita, verificável e executável; não é autorização para duplicar produto ou alterar CI.

## Baseline conhecido

- branch exclusiva criada limpa de `b46ebd9c84cacab6bd58d2fb2712879f6dabc164`, sem upstream;
- `main == origin/main` nesse SHA antes da branch;
- CI autoritativa da base: run `32000365336`, evento `push`, 10/10 + `CI required` success no SHA exato;
- `git status --short --branch` mostrou somente `## task/f7.2-test-matrix` após a criação;
- havia exatamente 1055 casos coletáveis; nenhum manifesto ou runner F7.2 existia;
- somente `docs/tasks/active/README.md` existia no diretório ativo;
- nenhum trabalho preexistente, stash ou worktree adicional foi encontrado.

## Contrato técnico congelado

1. `tests/f7_2_matrix.json` será a fonte canônica da seleção F7.2, em JSON estrito e determinístico,
   com versão, task ID, 12 camadas na ordem do plano e exatamente 42 requisitos nomeados.
2. Cada requisito apontará para ao menos um node ID pytest existente em `tests/unit/` ou `tests/e2e/`.
   Paths absolutos, travessia, arquivos fora de `tests/`, símbolos inexistentes e duplicação de node ID
   serão rejeitados antes da execução.
3. A matriz fará curadoria de provas já existentes; não copiará os corpos dos testes nem declarará
   cobertura por nome de arquivo apenas. O validador analisará o AST para comprovar cada função.
4. `tests/run_f7_2_matrix.py` carregará e validará a matriz antes de iniciar subprocesso, usará
   `sys.executable -m pytest`, argv e `shell=False`, raiz fixa do repositório e opção `--collect-only`.
5. O runner imprimirá contagem determinística de camadas, requisitos e node IDs únicos. Erro de
   schema, referência ou pytest retornará código não zero sem tentar fabricar sucesso.
6. `tests/unit/test_f7_2_matrix.py` congelará schema, ordem, requisitos, referências, falhas negativas
   e argv do runner sem executar pytest recursivamente.
7. `docs/test_matrix.md` explicará a relação entre plano, manifesto, runner, full suite e CI. Matriz
   selecionada não substituirá a regressão integral nem autorizará claims de cobertura F7.3.
8. Todos os node IDs selecionados devem executar em Windows e Linux sem rede, credenciais, skip ou
   xfail próprio da F7.2. O E2E externo da F7.1 será a prova obrigatória da camada E2E.
9. A camada recovery apontará para crash injection em checkpoints distintos, incluindo transição,
   model/tool effect, verificação, promoção e knowledge transaction; um único happy path não basta.
10. A F7.2 é infraestrutura de testes e documentação. Nenhum arquivo em `src/`, workflow de CI,
    dependência, lockfile, schema, default, versão ou comportamento de produção poderá mudar.

## Escopo congelado

### Implementação permitida

- `tests/f7_2_matrix.json` — manifesto canônico das 12 camadas e 42 requisitos;
- `tests/run_f7_2_matrix.py` — validador/runner multiplataforma por argv;
- `tests/unit/test_f7_2_matrix.py` — regressões do contrato e do runner;
- `docs/test_matrix.md` — uso, semântica e limitações da matriz.

### Documentação e estado permitidos

- `TASK.md`, `docs/tasks/README.md`, este dossiê e `README.md`;
- `tests/unit/test_documentation.py` e `tests/unit/test_task_ledger.py`.

### Reuso obrigatório sem edição

- todos os testes de produto já existentes referenciados pelo manifesto;
- `.github/workflows/ci.yml`, `pyproject.toml`, `uv.lock` e `tests/ci/smoke_wheel.py`;
- toda a árvore `src/`, compiler, defaults, contratos e schemas.

### Fora de escopo e proibido

- qualquer alteração em produto, dependências, CI, cobertura mínima, release, versão ou tags;
- F7.3–F7.5, publicação de pacote, changelog/licença ou claim de MVP;
- novo mock de produto, resposta sintética, rede, credencial ou provider live;
- marcar diretório inteiro como cobertura sem referências de teste verificáveis;
- skip/xfail, relaxamento ou remoção de teste que falhar, `shell=True`, comando em string ou path
  escolhido pelo manifesto para fora da raiz;
- reset/clean/checkout destrutivo, force-push, exclusão de ref, push, PR ou merge sem autorização.

Descoberta que exija arquivo fora do allowlist reabre o gate como `BLOCKED` e exige recongelamento
antes da edição.

## Critérios de aceite congelados

1. Manifesto contém exatamente 12 camadas, 42 requisitos, somente chaves conhecidas e node IDs únicos.
2. Cada referência resolve para arquivo Python sob `tests/unit` ou `tests/e2e` e função pytest real;
   referência ausente, externa, duplicada ou malformada falha fechada.
3. Contratos comprovam validação, compatibilidade e serialização; compiler comprova grafos válidos e
   inválidos.
4. Runtime comprova branches, retry, pause, resume e cancelamento; persistence comprova atomicidade,
   lock, replay e corrupção.
5. Models comprovam erros, timeout, tokens, structured output e tools; tools comprovam autorização,
   path guard, timeout e output limit.
6. Git comprova worktree, commit, divergência, promoção e revert; verification comprova pass, fail,
   missing tool e suite vazia.
7. Security comprova secrets, egress, trust e command injection; observability comprova sequence,
   hash, redaction e export.
8. E2E executa o ciclo completo em repositório externo; recovery injeta crash em checkpoints críticos.
9. `tests/run_f7_2_matrix.py --collect-only` e a execução integral do runner terminam com exit 0 e
   anunciam a cardinalidade canônica; nenhum requisito selecionado é skipped/xfail.
10. Testes do contrato, documentação/ledger, regressão integral, Ruff, mypy, compileall,
    `git diff --check`, build externo e smoke oficial offline ficam verdes.
11. Auditoria final prova que somente o allowlist mudou e que F7.3 não começou.

## Comandos de aceite congelados

```text
.\.venv\Scripts\python.exe -m pytest tests/unit/test_f7_2_matrix.py tests/unit/test_documentation.py tests/unit/test_task_ledger.py -q --basetemp <externo>
.\.venv\Scripts\python.exe tests/run_f7_2_matrix.py --collect-only
.\.venv\Scripts\python.exe tests/run_f7_2_matrix.py
.\.venv\Scripts\python.exe -m pytest tests/unit tests/e2e -q --basetemp <externo>
.\.venv\Scripts\python.exe -m ruff check .
.\.venv\Scripts\python.exe -m mypy src
.\.venv\Scripts\python.exe -m compileall -q src compiler tests
git diff --check
git diff --cached --check
.\.venv\Scripts\python.exe -m build --outdir <externo>
<python isolado> tests/ci/smoke_wheel.py
```

## Implementação entregue

- `tests/f7_2_matrix.json` contém as 12 camadas, os 42 requisitos e 46 node IDs únicos;
- `tests/run_f7_2_matrix.py` rejeita chave duplicada/desconhecida, ordem divergente, path externo,
  travessia, arquivo/função ausente e node ID repetido antes do subprocesso;
- o runner usa `sys.executable -m pytest`, argv, `shell=False`, `cwd` fixo e `basetemp` efêmero
  externo ao checkout; `HARNESS_F7_2_TEMP_PARENT` atende somente runtimes restritos;
- `tests/unit/test_f7_2_matrix.py` congela cardinalidade, recovery em cinco checkpoints, falhas
  negativas e o argv sem executar pytest recursivamente;
- `docs/test_matrix.md` documenta uso, cobertura, relação com regressão/CI e a fronteira F7.3.

O commit de implementação é `bdae858861a9c5294f90a231115b3ed930030117`. Nenhum arquivo em
`src/`, workflow, dependência, lockfile, schema, default ou versão mudou.

## Certificação local

Resultado terminal em `2026-08-17`:

```text
runner --collect-only: F7.2 matrix: 12 layers, 42 requirements, 46 unique pytest nodes
62 tests collected in 1.35s

runner integral: 62 passed in 83.45s
contrato + documentação + ledger: 45 passed in 1.21s
regressão integral: 1062 passed, 5 skipped, 6 subtests passed in 461.12s

Ruff: All checks passed!
mypy: Success: no issues found in 110 source files
compileall: exit 0
git diff --check: exit 0
uv build --offline: sdist e wheel 0.1.0 geradas com sucesso
smoke oficial offline: metadata/package/CLI 0.1.0; origin no ambiente uv isolado
```

Os cinco skips são os mesmos opt-ins/plataforma da base integral; a seleção F7.2 terminou 62/62 sem
skip ou xfail. A wheel do smoke foi regenerada em `dist/`, que é ignorado, e a origem importada ficou
fora do checkout.

### Iterações ambientais preservadas

1. O primeiro runner usou o temp padrão bloqueado pelo sandbox e terminou `12 passed, 50 errors` no
   setup; isso motivou o `basetemp` próprio, sem alterar teste selecionado.
2. Com temp gravável, o E2E parou fail-closed sem `uv`; com `uv` mas cache bloqueado, parou no cache.
3. Um `basetemp` sob `build/` foi rejeitado pela própria asserção de origem externa do E2E. A versão
   final exige temp externo e passou 62/62 com `uv`/cache offline autorizados.
4. `python -m build` isolado não pôde preparar o backend no temp restrito e `--no-isolation` provou
   que setuptools não estava no `.venv`; `uv build --offline` usou o cache local e fechou o gate.

Nenhuma dessas tentativas foi convertida em skip, xfail, relaxamento, rede ou alteração de produto.

## Rollback

- `checkpoint/f7.2-ready` preservará este contrato antes da primeira edição de implementação;
- manifesto, runner, teste e documentação ficarão em commits isolados, revertíveis com `git revert`;
- falha de qualquer requisito, referência, plataforma, regressão ou auditoria interrompe a tarefa sem
  remover o critério que falhou;
- rollback remove somente os novos arquivos do allowlist e inverte hunks de estado F7.2, preservando
  dossiê/evidência da falha; nunca usa reset, clean, force ou descarte amplo;
- verificação pós-rollback repete documentação/ledger, coleta baseline de 1055 testes, Ruff e diff-check.

## Checklist de liberação

- [x] regras, `TASK.md`, plano 1.1–1.2, Fase 7 e DEC-014 lidos;
- [x] baseline Git, CI terminal F7.1, branch, runtime, executor e horário comprovados;
- [x] ausência de matriz/runner reproduzida; 1055 testes existentes coletados;
- [x] 12 camadas e 42 requisitos contados sem reduzir o plano;
- [x] escopo permitido/proibido e reuso sem edição congelados;
- [x] critérios positivos, negativos, regressão, package e auditoria congelados;
- [x] rollback não destrutivo e executável congelado;
- [x] autorização explícita registrada;
- [x] commit documental e `checkpoint/f7.2-ready` criados;
- [x] manifesto, runner, teste e documento implementados no commit `bdae858`;
- [x] matriz 62/62, regressão 1062/5/6, quality, build e smoke certificados;
- [x] `checkpoint/f7.2-complete` definido para o commit documental desta certificação;
- [ ] push, PR, CI, merge e reconciliação executados sob autorização aplicável.

## Próxima ação

Commitar esta certificação, criar `checkpoint/f7.2-complete` somente local e parar antes de push/PR.
Promoção exige autorização aplicável e CI no SHA publicado. Não iniciar F7.3.
