# F4.C1 — Imutabilidade concorrente da publicação de snapshots

> **Gate:** `READY`
> **Lifecycle:** `COMPLETED_LOCAL / PROMOTION_PENDING`
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-10T23:11:37-03:00`
> **Branch:** `task/f4.c1-snapshot-publication-concurrency`
> **Python:** `.\.venv\Scripts\python.exe` — 3.12.13
> **Baseline:** `main == origin/main == 94641d27384d370faf013825e7e9955c721cf420`
> **Checkpoint:** `checkpoint/f4.c1-ready` → `03c286c327d0f470328a5265d394fca20ce9be6c`
> **Implementação:** `b4d212cb96a4b1b3335a467a7719b40856c30558`

## Problema comprovado

O contrato promovido da F4.1 exige que conteúdo divergente para o mesmo commit SHA seja conflito e
nunca substitua o snapshot existente. No baseline, `SnapshotManager.save_snapshot()` verifica a
existência antes de publicar e `_atomic_replace_text()` usa `os.replace`; não há claim exclusivo entre
essas operações. Duas publicações que observam o destino ausente podem, portanto, concluir com sucesso
e a segunda substituir o snapshot já validado pela primeira.

Reprodução determinística executada em `2026-08-10T23:07:00-03:00`, sem alterar o repositório:

```text
command: .\.venv\Scripts\python.exe -  # script concorrente com barrier antes de _atomic_replace_text
exit: 0
first=success
second=success
final=second
```

O script fez os dois writers alcançarem a publicação depois de ambos observarem o destino ausente,
permitiu que `first` publicasse e concluísse sua validação e só então liberou `second`. O resultado
prova que o vencedor foi sobrescrito. A localização afetada é
`src/ai_engineering_harness/indexer/snapshot_manager.py`, em `save_snapshot()` e
`_atomic_replace_text()`; o teste sequencial existente em `tests/unit/test_structural_index.py` não
exercita essa janela.

Esta evidência negativa é posterior à promoção. F4.1–F4.4 permanecem historicamente `PROMOTED`, mas o
estado operacional é `POST_PROMOTION_BLOCKED` até correção e recertificação. F4.5 não pode iniciar.

## Baseline conhecido

| Evidência | Resultado observado |
|---|---|
| Workspace | `C:\Users\walla\OneDrive\Desktop\ai-engineering-harness` |
| Git inicial | worktree limpa; `main...origin/main`; zero ahead/behind |
| SHA | `HEAD == main == origin/main == 94641d27384d370faf013825e7e9955c721cf420` |
| CI do SHA | run `31447628152`, evento `push`, status `Success`; matrizes package 2/2, quality 4/4, tests 4/4 e `CI required` verde, total 11/11 |
| Branch isolada | `task/f4.c1-snapshot-publication-concurrency`, criada diretamente do baseline |
| Mudanças preexistentes | nenhuma; trabalho alheio inexistente no diff e deve permanecer preservado |
| Runtime | `.\.venv\Scripts\python.exe` 3.12.13 |
| Toolchain | o path `build/f0.6-tools/uv/bin/uv.exe` registrado anteriormente não existe neste checkout; nenhuma instalação ou substituição silenciosa será feita |

O gate começou `BLOCKED`. Após registrar problema, baseline, CI, escopo, aceite, rollback, executor,
runtime e horário, passou a `READY`. O checkpoint nomeado acima deve existir antes do primeiro arquivo
de implementação.

## Contrato corretivo congelado

1. A publicação usa um claim atômico exclusivo no path canônico do SHA; nenhum caminho concorrente
   pode substituir um destino que já exista.
2. Duas gravações concorrentes e byte-idênticas para o mesmo SHA são idempotentes: ambas retornam o
   path canônico e o documento final é exatamente o conteúdo validado.
3. Entre duas gravações concorrentes divergentes para o mesmo SHA, exatamente uma publica; a outra
   recebe `SnapshotConflictError` depois de validar o documento vencedor.
4. O snapshot vencedor nunca é sobrescrito, inclusive quando o perdedor já preparou e sincronizou seu
   arquivo temporário antes do claim.
5. Falha ao preparar, sincronizar ou criar o claim não deixa snapshot parcial nem temporário e retorna
   `SnapshotWriteError`, salvo `FileExistsError`, que segue o fluxo tipado de idempotência/conflito.
6. SHA, schema, digest, status, JSON canônico, boundary contra symlinks e consumidores F4.1–F4.4
   permanecem inalterados ou fortalecidos.

## Escopo congelado

### Permitido

- `src/ai_engineering_harness/indexer/snapshot_manager.py` — substituir a publicação com overwrite por
  criação atômica exclusiva no mesmo diretório;
- `tests/unit/test_structural_index.py` — provas determinísticas de idempotência e conflito concorrentes,
  vencedor imutável, cleanup e falha de publicação;
- `TASK.md`, `README.md`, `docs/tasks/README.md` e este dossiê — estado operacional honesto, baseline,
  resultados e handoff;
- `tests/unit/test_task_ledger.py` e `tests/unit/test_documentation.py` — regressões estritamente
  necessárias ao novo estado corrente.

### Proibido

- reescrever qualquer dossiê em `docs/tasks/completed/` ou apagar a falha histórica;
- alterar contratos/schemas/digests, indexador AST F4.2, context sufficiency F4.3, planner F4.4,
  `GraphExecutor`, FSM, persistence storage F2, defaults, policies ou gates F4.5+;
- adicionar dependência, lockfile, workflow/CI, backend, processo, thread ou serviço de produção;
- introduzir retry ilimitado, espera não limitada, fallback com overwrite, snapshot parcial ou sucesso
  sintético;
- publicar branch/tag, abrir/mesclar PR, excluir refs, force-push ou bypass sem autorização nominal nova.

Novo arquivo de produto, efeito, dependência ou critério interrompe a implementação e exige
recongelamento. Critério falho não pode ser removido, skipado, xfailado ou enfraquecido.

## Critérios de aceite congelados

```yaml
acceptance:
  concurrent_publication:
    command: >-
      .\.venv\Scripts\python.exe -m pytest tests/unit/test_structural_index.py -q
      -k "concurrent or idempotent or conflict or atomic"
    expected: >-
      gravações idênticas concorrentes são ambas idempotentes; gravações divergentes produzem um
      vencedor e SnapshotConflictError no perdedor; bytes vencedores permanecem inalterados; falha
      não deixa snapshot parcial ou temporário
  f4_direct_acceptance:
    command: >-
      .\.venv\Scripts\python.exe -m pytest
      tests/unit/test_structural_index.py tests/unit/test_python_ast_indexer.py
      tests/unit/test_context_sufficiency.py tests/unit/test_context_lifecycle.py
      tests/unit/test_planner.py tests/unit/test_planning_lifecycle.py -q
    expected: "aceite direto F4.1–F4.4 integralmente verde, sem remover testes"
  f4_compatibility:
    command: >-
      .\.venv\Scripts\python.exe -m pytest
      tests/unit/test_phase4.py tests/unit/test_agent_centric.py tests/unit/test_cli_runtime.py
      tests/unit/test_public_module_imports.py tests/unit/test_execution_lifecycle.py
      tests/unit/test_graph_executor.py tests/unit/test_model_routing.py tests/unit/test_contracts.py
      tests/e2e/test_full_lifecycle.py -q
    expected: "consumidores e fronteiras promovidas F4.1–F4.4 permanecem verdes"
  documentation:
    command: >-
      .\.venv\Scripts\python.exe -m pytest
      tests/unit/test_task_ledger.py tests/unit/test_documentation.py tests/unit/test_encoding.py -q
    expected: "estado POST_PROMOTION_BLOCKED, gate F4.C1, links, Markdown e UTF-8 coerentes"
  regression:
    command: ".\.venv\Scripts\python.exe -m pytest tests/unit tests/e2e -q"
    expected: "suíte integral verde; somente skips live opt-in preexistentes"
  quality:
    command: >-
      .\.venv\Scripts\python.exe -m mypy src;
      .\.venv\Scripts\python.exe -m mypy --platform linux src;
      .\.venv\Scripts\python.exe -m ruff check src compiler tests;
      .\.venv\Scripts\python.exe -m compileall -q src compiler tests;
      git diff --check
    expected: "todos exit 0, sem suppression ou relaxamento"
  package_ci_parity:
    command: >-
      .\.venv\Scripts\python.exe -m build;
      .\.venv\Scripts\python.exe tests/ci/smoke_wheel.py
    expected: >-
      sdist/wheel e smoke isolado verdes; CI remota do novo head continua pendente até publicação
      explicitamente autorizada
  scope:
    command: "git diff --name-only checkpoint/f4.c1-ready e auditoria das áreas proibidas"
    expected: "somente a allowlist F4.C1; produto F4.2–F4.4, F4.5+, deps, CI e schemas byte-idênticos"
```

Falha em qualquer critério mantém `POST_PROMOTION_BLOCKED`. Sucesso local permite apenas
`COMPLETED_LOCAL / PROMOTION_PENDING`; não restaura o estado operacional positivo antes de PR, merge,
CI pós-merge e reconciliação administrativa realmente observados.

## Rollback

### Gatilhos

- qualquer writer concorrente substituir snapshot já publicado;
- gravação idêntica concorrente falhar como conflito ou divergente não produzir `SnapshotConflictError`;
- publicação parcial, temporário residual, symlink traversal ou regressão de digest/schema/status;
- solução exigir dependência, lockfile, CI, backend, espera ilimitada ou arquivo proibido;
- aceite F4.1–F4.4, regressão, quality, package/smoke, documentação ou escopo falhar.

### Procedimento não destrutivo

Parar e preservar diff/saídas. Antes do commit de implementação, inverter somente os hunks F4.C1 com
`apply_patch`. Depois de commit, usar `git revert` apenas nos commits exclusivos desta branch. Nunca
usar `git reset --hard`, `git clean`, checkout amplo nem apagar snapshots/estado do usuário.

### Verificação pós-rollback

Confirmar `git status --short --branch`, checkpoint `checkpoint/f4.c1-ready`, `git diff --check` e zero
diff de produto contra o checkpoint; repetir o teste concorrente de reprodução, documentação, aceite
F4.1–F4.4, regressão, quality e package/smoke. A reprodução voltar a expor a lacuna é resultado
esperado do rollback, mas mantém F4.5 bloqueada.

## Checklist de liberação

```text
[x] problema comprovado por reprodução determinística no SHA promovido
[x] baseline Git e CI remota 11/11 do SHA exato registrados
[x] escopo permitido/proibido e contrato concorrente congelados
[x] aceite F4.1–F4.4, regressão, quality, package/CI parity e escopo congelados
[x] rollback não destrutivo, executor, runtime e horário registrados
[x] F4.5 e toda implementação futura permanecem fora do gate
```

## Evidências de execução

### Implementação observada

`_atomic_replace_text()` foi substituída por `_atomic_create_text()`. O writer prepara o JSON canônico
em arquivo temporário no mesmo diretório, executa `flush` + `fsync` e chama `os.link(temp, destino)`.
O hard link é o claim atômico: cria o path final somente quando ausente e nunca substitui uma entrada
existente. Depois do claim, o temporário é removido e o diretório é sincronizado quando suportado.

Se outro writer venceu, `FileExistsError` leva à leitura íntegra do vencedor: conteúdo igual retorna o
mesmo path de forma idempotente; conteúdo divergente gera `SnapshotConflictError`. Qualquer outro
`OSError` continua virando `SnapshotWriteError`. Não há lock persistente, thread/processo de produção,
espera, retry, dependência ou alteração de schema.

Os testes usam duas instâncias de `SnapshotManager` e uma barrier exatamente antes de `os.link`,
garantindo que ambos os temporários estejam prontos para disputar o mesmo destino. A prova divergente
identifica o writer vencedor, exige um único `SnapshotConflictError`, relê o símbolo vencedor e compara
os bytes antes/depois de nova tentativa perdedora. A prova idêntica exige sucesso para ambos.

### Validações executadas

| Critério | Resultado observado |
|---|---|
| Checkpoint READY | commit/tag local `03c286c` / `checkpoint/f4.c1-ready`, criado antes do primeiro hunk de produto |
| Foco concorrente | `4 passed, 4 deselected`; idênticos idempotentes, divergente conflitante, vencedor imutável e falha atômica sem resíduo |
| Arquivo estrutural completo | `8 passed` |
| Aceite direto F4.1–F4.4 | `60 passed` |
| Consumidores/fronteiras | `106 passed` fora do sandbox; a primeira execução teve `105 passed` e apenas `PermissionError` no path contratual de AppData |
| Documentação/encoding | `24 passed, 6 subtests passed` |
| Regressão integral | `702 passed, 2 skipped, 6 subtests passed`; skips exclusivamente Serena/modelo live opt-in |
| mypy Windows | 105 arquivos, zero issues; duas notas preexistentes de corpos não tipados em `lease_manager.py` |
| mypy Linux | 105 arquivos, zero issues; mesmas duas notas preexistentes |
| Ruff / compileall / diff | todos verdes; `git diff --check` exit 0 |
| Build | sdist e wheel `0.1.0` gerados pelo build isolado |
| Smoke da wheel | metadata/package/CLI `0.1.0`; import originado do cache isolado do `uv`, fora do workspace |
| Escopo | antes do fechamento documental, somente `snapshot_manager.py` e `test_structural_index.py`; F4.2–F4.4, F4.5+, contracts, persistence, defaults, deps e CI byte-idênticos |

O pytest dentro do sandbox não podia enumerar seu temporário global; os critérios foram repetidos com
`--basetemp` fora do repositório. `test_sandbox_provider_by_platform` precisava acessar o path
contratual em AppData; o bloco e a regressão foram repetidos fora do sandbox, sem mudança de código ou
teste. O build isolado inicialmente não pôde instalar `setuptools`; repetido fora do sandbox, concluiu.
O smoke recebeu no `PATH` somente o `uv` versionado do projeto e ficou verde. Essas adaptações mudaram
apenas confinamento ambiental, nunca critérios ou expectativas.

### Commits e estado local

| Commit/checkpoint | Conteúdo |
|---|---|
| `03c286c` / `checkpoint/f4.c1-ready` | problema, baseline, CI, contrato, escopo, aceite e rollback congelados |
| `b4d212c` | claim atômico exclusivo, tratamento tipado e regressões concorrentes determinísticas |
| `checkpoint/f4.c1-complete` | fechamento documental e handoff local; criado no commit final desta branch |

A implementação está concluída localmente e o gate passa a `COMPLETED_LOCAL / PROMOTION_PENDING`.
Isso não restaura ainda o estado operacional positivo: F4.1–F4.4 permanecem historicamente promovidas,
mas `POST_PROMOTION_BLOCKED` continua até branch/PR, CI do head final, merge, CI pós-merge e
reconciliação administrativa realmente observados. F4.5 continua proibida.
