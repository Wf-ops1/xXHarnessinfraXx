# F6.1 — Schema único de eventos

> **Gate:** `READY`
> **Lifecycle:** `REPAIR_ACTIVE`
> **Branch:** `task/f6.1-unified-event-schema`
> **Baseline:** `29e8a9751c2cc1bf4e45fa530d971e969f22342f`
> **Checkpoint READY:** `checkpoint/f6.1-ready` → `e149fb320385b5fe1c91d85ef828c38350ca6cc3` (local)
> **Produto:** `c9e41c4` — `feat(f6.1): unify canonical event schema`
> **Checkpoint R1 READY:** `checkpoint/f6.1-r1-ready` → `eea6baa5bf042c6f76addb673ac5d019d29cabf9`; somente local
> **Checkpoint COMPLETE:** a criar no commit documental de certificação; somente local
> **Recertificação anterior invalidada em:** `2026-08-15T01:22:15-03:00`
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-15T00:18:48-03:00`
> **Autorização:** prompt de retomada anexado — preparar/congelar o primeiro gate F6 e continuar localmente dentro do escopo
> **Runtime:** `.\.venv\Scripts\python.exe` — Python 3.12.13

## Problema comprovado

A F6.1 exige um único envelope de evento com `event_id`, `execution_id`, `sequence_number`,
`event_type`, `timestamp`, `graph_name`, `node_id` quando aplicável, `attempt`, `actor`, `details`
redigidos, `previous_hash` e `current_hash`. O baseline possui dois modelos concorrentes e nenhum
deles satisfaz sozinho esse contrato:

- `src/ai_engineering_harness/contracts/events/execution_event.py::ExecutionEvent` é o envelope
  efetivamente persistido pelo journal canônico F2.2, mas não possui `sequence_number`, `graph_name`,
  `node_id`, `attempt`, `actor` nem `details`; usa `payload`;
- `src/ai_engineering_harness/observability/event_schema.py::HarnessTraceEvent` possui parte dos
  metadados de tracing, porém não possui `sequence_number`, `attempt`, `actor`, `previous_hash` nem
  `current_hash` e não é consumido por nenhum caminho de produto;
- `AuditTrailManager` mantém outro formato de journal. Seu fortalecimento pertence à F6.2 e não pode
  ser absorvido silenciosamente por esta tarefa.

Probe read-only reproduzível:

```text
.\.venv\Scripts\python.exe -c "from ai_engineering_harness.contracts.events import ExecutionEvent; from ai_engineering_harness.observability.event_schema import HarnessTraceEvent; required={'event_id','execution_id','sequence_number','event_type','timestamp','graph_name','node_id','attempt','actor','details','previous_hash','current_hash'}; execution=set(ExecutionEvent.model_fields); trace=set(HarnessTraceEvent.model_fields); print('ExecutionEvent fields:', sorted(execution)); print('ExecutionEvent missing:', sorted(required-execution)); print('HarnessTraceEvent fields:', sorted(trace)); print('HarnessTraceEvent missing:', sorted(required-trace)); print('same schema:', execution == trace)"
```

Resultado observado, exit code `0`:

```text
ExecutionEvent fields: ['current_hash', 'event_id', 'event_schema_version', 'event_type', 'execution_id', 'payload', 'previous_hash', 'timestamp']
ExecutionEvent missing: ['actor', 'attempt', 'details', 'graph_name', 'node_id', 'sequence_number']
HarnessTraceEvent fields: ['details', 'event_id', 'event_type', 'execution_id', 'graph_name', 'node_id', 'timestamp']
HarnessTraceEvent missing: ['actor', 'attempt', 'current_hash', 'previous_hash', 'sequence_number']
same schema: False
```

Essa evidência negativa prevalece sobre os testes verdes do contrato 1.0: eles certificam o envelope
anterior, não o contrato exigido pela F6.1.

## Baseline conhecido

```yaml
baseline:
  branch_before_task: main
  branch: task/f6.1-unified-event-schema
  head: 29e8a9751c2cc1bf4e45fa530d971e969f22342f
  main: 29e8a9751c2cc1bf4e45fa530d971e969f22342f
  origin_main: 29e8a9751c2cc1bf4e45fa530d971e969f22342f
  status: clean antes da criação da branch; nenhuma mudança preexistente
  upstream_ci: run 31859624571, push, success, 11/11 no SHA exato de main
  previous_admin_pr: 68, head 5b8e5585f2fd729787589feeb0ed9f4d217e6e7f, merge 29e8a9751c2cc1bf4e45fa530d971e969f22342f
  python_command: .\.venv\Scripts\python.exe
```

O PR administrativo #68 foi observado como `Merged`, com 11 checks aprovados. A CI pós-merge
`31859624571` foi observada como `Success`, evento `push`, no SHA exato `29e8a975`; ela é evidência
terminal da F5.C1 e baseline deste gate. Pela DEC-014, não existe reconciliação recursiva desse PR.

Baseline focado:

- R0 inválido: `14 passed, 82 errors`; todos os erros vieram de `PermissionError` antes da execução
  dos testes que usam `tmp_path`, pois o pytest tentou criar `AppData/Local/Temp/pytest-of-walla` fora
  da raiz gravável. Esse resultado não é evidência negativa de produto;
- R1 válido, com `TEMP`, `TMP`, `LOCALAPPDATA`, `--basetemp` e cache confinados a uma raiz gravável:
  `96 passed em 9.10s` para contrato, storage, FSM e execução orientada a eventos.

Comando lógico do R1:

```text
.\.venv\Scripts\python.exe -m pytest tests/unit/test_contracts.py tests/unit/test_state_storage.py tests/unit/test_state_machine.py tests/e2e/test_event_sourced_execution.py -q --basetemp=<raiz-gravável> -p no:cacheprovider
```

## Dependências

- F2.1–F2.6: `ExecutionRecord`, journal canônico, storage, FSM, executor e retomada;
- F3.1–F3.8: model turns, tools, worktree e promoção que já produzem evidência durável;
- F4.1–F4.8: contexto, plano, gates, verificação e repair loop;
- F5.1–F5.7/F5.C1: configuração efetiva, policy, budget, redaction, approval, cancelamento e rollback;
- DEC-014: F5.C1 administrativamente encerrada; o merge #68 compõe o baseline sem fechamento
  recursivo.

Nenhuma outra decisão é referenciada diretamente pela definição da F6.1 no plano.

## Escopo congelado

### Permitido

- `src/ai_engineering_harness/contracts/events/`: elevar o envelope canônico para uma versão nova,
  definir a taxonomia mínima F6.1 e remover a duplicidade de modelos de tracing;
- `src/ai_engineering_harness/contracts/registry.py` e exports diretamente afetados;
- `src/ai_engineering_harness/observability/event_schema.py`: tornar o nome legado um alias/import do
  contrato canônico ou removê-lo como implementação independente; não alterar o audit manager F6.2;
- `src/ai_engineering_harness/persistence/base.py` e
  `src/ai_engineering_harness/persistence/atomic_file.py`: persistir o envelope novo, atribuir os
  metadados cuja autoridade pertence ao journal e manter o hash canônico cobrindo todos os campos;
- produtores canônicos atuais em `runtime/state_machine.py`, `runtime/graph_executor.py`,
  `runtime/execution_lifecycle.py` e `governance/budget.py`, apenas para construir o envelope único e
  fornecer metadados verdadeiros;
- uso da redaction F5.5/F5.C1 já existente para impedir segredo controlado em `details`; mudança no
  algoritmo de redaction exige parar e recongelar, pois não está autorizada por este gate;
- testes unitários/e2e diretamente correspondentes, inclusive um arquivo focado novo de schema/taxonomia;
- `TASK.md`, `docs/tasks/README.md`, `README.md`, este dossiê e sentinelas documentais necessárias para
  registrar o gate e a verdade terminal da F5.C1.

### Excluído e proibido

- `src/ai_engineering_harness/observability/audit.py`, lock/gaps/duplicatas/HMAC/export do journal
  legado: F6.2;
- `evidence.json` e `harness evidence --verify`: F6.3;
- doctor, status/inspect/events CLI, recovery por fault injection e knowledge transaction: F6.4–F6.7;
- alegar que todo evento mínimo já possui um produtor live quando o backend correspondente ainda é
  explícito, injetado ou opt-in; a F6.1 tipa a taxonomia e integra somente caminhos operacionais reais;
- refatoração estrutural ampla, troca de storage, API não relacionada, defaults, dependências,
  lockfile, workflow de CI, provider, MCP, worktree/promoção/rollback ou políticas;
- enfraquecer `extra="forbid"`, JSON nativo, UTC, hash chain, CAS/fencing, redaction ou qualquer teste;
- push, PR, merge, tag remota, exclusão de branch/ref, force-push ou bypass sem autorização explícita.

## Critérios de aceite congelados

### Contrato e taxonomia

- existe um único `BaseModel` canônico para eventos operacionais; `HarnessTraceEvent`, se preservado,
  é identidade/alias desse contrato, não outro schema;
- o schema versionado contém todos os campos F6.1; `node_id` é opcional somente quando não aplicável,
  e os demais metadados são validados com tipos estritos e invariantes documentados;
- a mudança incompatível não permanece rotulada como schema `1.0`;
- a taxonomia tipada contém, no mínimo, criação de execução/workspace, contexto montado/bloqueado,
  plano, node start/success/failure, model request success/failure, tool authorization/denial/start/
  success, gate start/success, retry, approval request/grant/reject/expire, promotion start/success/
  failure, knowledge sync, execution success/failure/cancel, rollback request/success/failure;
- tipo desconhecido, campo extra, timestamp não UTC, número/tentativa inválidos ou `details` não JSON
  falham de forma tipada e fechada.

### Persistência, redaction e compatibilidade operacional

- todo evento persistido contém o envelope novo, sequence number positivo, hashes não nulos e hash
  atual calculado sobre a serialização canônica completa;
- append concorrente continua serializado pelo lock/fencing existente e não aceita hash fornecido pelo
  chamador; detecção explícita de gaps/duplicatas históricos permanece para F6.2;
- detalhes e texto sob chaves sensíveis são redigidos antes da persistência; um segredo controlado não
  aparece no objeto, JSON canônico ou journal;
- os produtores atuais preservam execution ID, graph, node, attempt e actor reais, sem valores
  sintéticos que aleguem uma integração inexistente;
- replay, FSM, budget, approval, cancelamento, rollback, promoção, gates e resume mantêm a semântica
  certificada nas fases anteriores.

### Comandos exatos

```text
.\.venv\Scripts\python.exe -m pytest tests/unit/test_event_schema.py tests/unit/test_contracts.py tests/unit/test_state_storage.py tests/unit/test_state_machine.py tests/unit/test_graph_executor.py tests/unit/test_budget.py tests/unit/test_execution_lifecycle.py tests/unit/test_context_lifecycle.py tests/unit/test_planning_lifecycle.py tests/unit/test_verification_lifecycle.py tests/unit/test_tool_loop.py tests/unit/test_cancellation.py tests/unit/test_rollback_manager.py tests/e2e/test_event_sourced_execution.py tests/e2e/test_execution_resume.py tests/e2e/test_durable_budget.py tests/e2e/test_safe_promotion.py -q
.\.venv\Scripts\python.exe -m pytest tests/unit/test_documentation.py tests/unit/test_task_ledger.py tests/unit/test_encoding.py -q
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m ruff check .
.\.venv\Scripts\python.exe -m mypy src
.\.venv\Scripts\python.exe -m compileall -q src tests
.\.venv\Scripts\python.exe -m build --wheel --outdir dist
uv run --offline python tests/ci/smoke_wheel.py
git diff --check
```

Para pytest no sandbox atual, os mesmos comandos devem definir `TEMP`, `TMP`, `LOCALAPPDATA`,
`--basetemp` e `-p no:cacheprovider` em uma raiz gravável. Isso muda somente a infraestrutura de teste,
não seleção, expectativa ou resultado.

Resultado esperado: exit code `0` em todos os comandos; nenhum teste anteriormente aprovado removido,
ignorado ou relaxado. Condições de falha: qualquer campo obrigatório ausente, dois modelos concorrentes,
schema incompatível ainda `1.0`, segredo controlado persistido, metadado inventado, hash/replay/CAS
regredido, claim automático indevido, teste/quality/build/smoke falho ou diff fora do escopo.

## Resultado local certificado

- `ExecutionEvent` é o único `BaseModel` operacional, versionado como schema `2.0`; o nome legado
  `HarnessTraceEvent` é identidade desse mesmo tipo. O probe positivo retornou `missing: []` e
  `same schema: True`;
- `EventType` fecha a taxonomia canônica e contém integralmente a taxonomia mínima congelada. Tipos
  desconhecidos, UTC ambíguo, campo extra e metadados node-scoped inválidos falham fechados;
- o storage atribui `sequence_number` positivo sob o lock existente, valida `graph_name` contra o
  execution record e calcula o hash sobre o envelope completo. Sequência ou hashes fornecidos pelo
  chamador são recusados;
- FSM, graph executor, lifecycle e budget boundary fornecem graph/node/attempt/actor reais. `details`
  são JSON-native e redigidos antes da serialização; o segredo controlado não apareceu no objeto,
  JSON canônico nem journal;
- F6.2–F6.7, algoritmo de redaction, dependências, defaults, CI e APIs não relacionadas permaneceram
  intocados. A taxonomia não é apresentada como prova de backend live inexistente.

Certificação executada:

```text
probe positivo: schema 2.0; missing: []; same schema: True
matriz focada congelada: 282 passed em 248.90s
sentinelas documentais: 35 passed, 6 subtests passed em 29.16s
suíte integral: 929 passed, 5 skipped, 6 subtests passed em 468.36s
Ruff: All checks passed
mypy: Success, 107 source files
compileall: exit code 0
git diff --check: exit code 0
wheel: ai_engineering_harness-0.1.0-py3-none-any.whl construído
smoke oficial offline: metadata/package/CLI 0.1.0, origem fora do workspace
```

As primeiras tentativas de build/smoke foram inválidas por infraestrutura: build isolado sem rede e
temp externo; depois, cache global e leitura do wheel bloqueados pelo sandbox. As repetições autorizadas
e confinadas passaram sem alteração de código ou expectativa. Os cinco skips integrais são os mesmos
condicionais de Serena live, symlinks POSIX e provider live já documentados.

## Reabertura R1 — contratos knowledge ainda nomeados como eventos

A revisão posterior encontrou evidência negativa que prevalece sobre a certificação acima. O registry
canônico ainda expõe `KnowledgeSyncEvent` e `KnowledgeUpdateEvent` como `BaseModel` independentes;
`KnowledgeSyncCompleted` e `KnowledgeSyncFailed` também permanecem no pacote `contracts/events/` sem
serem classificados explicitamente como detalhes/resultados. O probe abaixo mostrou que somente
`ExecutionEvent` possui o envelope F6.1:

```text
ExecutionEvent missing= []
KnowledgeSyncEvent missing= [actor, attempt, current_hash, details, event_id, event_type,
  execution_id, graph_name, node_id, previous_hash, sequence_number, timestamp]
KnowledgeUpdateEvent missing= [actor, attempt, current_hash, details, event_type, graph_name,
  node_id, previous_hash, sequence_number]
KnowledgeSyncCompleted missing= [actor, attempt, current_hash, details, event_id, event_type,
  graph_name, node_id, previous_hash, sequence_number, timestamp]
KnowledgeSyncFailed missing= [actor, attempt, current_hash, details, event_id, event_type,
  graph_name, node_id, previous_hash, sequence_number, timestamp]
```

A correção R1 permanece dentro do escopo já congelado para `contracts/events/`, registry, exports e
testes correspondentes. Os contratos knowledge serão renomeados/reclassificados como `Details`, com
aliases apenas no resolvedor de referências legadas já existente; nenhum segundo envelope operacional
será preservado. Uma regressão estrutural exigirá que `ExecutionEvent` seja o único modelo interno cujo
nome termine em `Event`. F6.2–F6.7, dependências, CI, defaults e algoritmos de redaction continuam fora
do escopo. A autorização explícita para a correção veio do usuário em `2026-08-15T01:22:15-03:00`.

## Rollback

### Gatilhos

- o envelope novo não puder ser persistido e retomado sem ambiguidade;
- redaction, hash chain, CAS/fencing ou semântica de uma fase anterior regredir;
- a implementação exigir F6.2–F6.7, nova dependência, mudança de CI/default ou refatoração ampla;
- qualquer critério congelado falhar.

### Procedimento não destrutivo

Interromper imediatamente, registrar a evidência e inverter apenas commits/hunks exclusivos da F6.1
com `git revert` ou patch inverso explícito. Preservar `main`/baseline `29e8a975`, trabalho alheio e o
dossiê append-only. Não usar `git reset --hard`, checkout destrutivo ou sobrescrita ampla.

### Verificação pós-rollback

```text
git status --short --branch
git diff --check
.\.venv\Scripts\python.exe -m pytest tests/unit/test_contracts.py tests/unit/test_state_storage.py tests/unit/test_state_machine.py tests/e2e/test_event_sourced_execution.py -q
```

O rollback funcional retorna ao baseline `96 passed`, mas restaura a lacuna comprovada e, portanto,
recoloca a tarefa em `BLOCKED`; não pode ser apresentado como conclusão.

## Checklist de liberação

- [x] problema comprovado com probe reproduzível e localização exata
- [x] baseline Git, upstream/CI e mudanças preexistentes registrados
- [x] escopo permitido/proibido e dependências congelados
- [x] critérios exatos, resultados esperados e condições de falha congelados
- [x] rollback não destrutivo definido
- [x] executor único, runtime, autorização e horário registrados
- [x] R0 de ambiente invalidado sem atribuir falha ao produto
- [x] baseline focado R1 válido: `96 passed em 9.10s`
- [x] evidência terminal da F5.C1 classificada sem reconciliação recursiva
- [x] baseline Git conhecido
- [x] escopo congelado
- [x] rollback verificável
- [x] checkpoint local `checkpoint/f6.1-ready` criado no commit deste gate
- [x] primeiro arquivo de implementação alterado somente depois do checkpoint
- [x] implementação localizada registrada em `c9e41c4`
- [x] probe positivo, matriz focada, suíte integral, quality, wheel e smoke verdes
- [x] estado anterior classificado como `COMPLETED_LOCAL / PROMOTION_PENDING`
- [x] evidência negativa knowledge registrada e estado reaberto como `REPAIR_ACTIVE`
- [x] checkpoint local `checkpoint/f6.1-r1-ready` criado antes da correção
- [ ] contratos knowledge reclassificados e regressão de unicidade ampliada
- [ ] aceite completo repetido depois da correção R1
- [ ] checkpoint local `checkpoint/f6.1-complete` criado no commit de certificação
- [ ] push/PR/checks/merge autorizados e concluídos

## Diário append-only

- `2026-08-15T00:18:48-03:00` — `main == origin/main == 29e8a975`, workspace clean; branch
  `task/f6.1-unified-event-schema` criada localmente sem upstream.
- `2026-08-15T00:18:48-03:00` — PR #68/merge `29e8a975` e CI pós-merge `31859624571` observados como
  encerramento terminal da F5.C1; nenhuma reconciliação recursiva aberta.
- `2026-08-15T00:18:48-03:00` — probe confirmou dois schemas e campos F6.1 ausentes.
- `2026-08-15T00:18:48-03:00` — R0 inválido por temp bloqueado; R1 confinado passou `96` testes.
- `2026-08-15T00:18:48-03:00` — problema, baseline, dependências, escopo, aceite, falha, rollback,
  executor e runtime congelados; nenhum arquivo de produto alterado.
- `2026-08-15T00:18:48-03:00` — sentinelas documentais passaram `35 passed, 6 subtests passed`;
  Ruff e `git diff --check` verdes, sem diff em `src/`.
- `2026-08-15T00:18:48-03:00` — commit documental `e149fb320385b5fe1c91d85ef828c38350ca6cc3`
  criado e apontado pelo checkpoint local `checkpoint/f6.1-ready`; nenhum push/tag remoto executado.
- `2026-08-15T01:11:37-03:00` — implementação localizada concluída no commit `c9e41c4`: envelope
  canônico 2.0, taxonomia fechada, alias único, sequência sob lock, hashes do envelope completo e
  metadados reais nos produtores atuais; F6.2–F6.7 permaneceram fora do diff.
- `2026-08-15T01:11:37-03:00` — probe positivo sem campos ausentes; matriz focada `282 passed`;
  sentinelas `35 passed, 6 subtests`; full `929 passed, 5 skipped, 6 subtests`.
- `2026-08-15T01:11:37-03:00` — Ruff, mypy em 107 arquivos, compileall, diff-check, wheel 0.1.0 e
  smoke oficial offline verdes. Falhas anteriores de build/smoke foram classificadas como sandbox/
  bootstrap e substituídas por execuções válidas.
- `2026-08-15T01:11:37-03:00` — tarefa classificada `COMPLETED_LOCAL / PROMOTION_PENDING`; nenhum
  push, PR, merge, tag remota, remoção de ref ou bypass executado.
- `2026-08-15T01:15:26-03:00` — a primeira validação documental pós-certificação rejeitou apenas a
  sentinela que ainda exigia `IMPLEMENTATION_NOT_STARTED`; a expectativa foi elevada para exigir
  `c9e41c4`, `282`, `929` e `COMPLETED_LOCAL / PROMOTION_PENDING`, sem remover nenhuma asserção
  histórica. A repetição passou `35 passed, 6 subtests passed em 24.17s`; Ruff, compileall e
  diff-check permaneceram verdes.
- `2026-08-15T01:22:15-03:00` — revisão read-only encontrou `KnowledgeSyncEvent` e
  `KnowledgeUpdateEvent` como envelopes nominais independentes no registry; o teste F6.1 cobria apenas
  o alias `HarnessTraceEvent`. A evidência negativa invalidou `COMPLETED_LOCAL` antes de qualquer
  promoção e reabriu o gate como `REPAIR_ACTIVE`.
- `2026-08-15T01:22:15-03:00` — usuário autorizou a correção. Escopo R1 reconfirmado nos paths já
  permitidos; checkpoint `checkpoint/f6.1-r1-ready` será criado antes do primeiro arquivo de produto.
- `2026-08-15T01:22:15-03:00` — a primeira sentinela de reabertura rejeitou a remoção acidental da
  menção histórica `POST_PROMOTION_BLOCKED` da F5.C1; a frase foi restaurada sem alterar o estado
  promovido daquela tarefa.
- `2026-08-15T01:22:15-03:00` — reabertura registrada no commit
  `eea6baa5bf042c6f76addb673ac5d019d29cabf9`, apontado pelo checkpoint local
  `checkpoint/f6.1-r1-ready`; nenhum arquivo de produto R1 havia sido alterado.
