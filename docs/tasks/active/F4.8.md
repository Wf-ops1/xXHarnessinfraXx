# F4.8 — Repair loop orientado pelos gates

> **Gate:** `READY`
> **Lifecycle:** `PR_OPEN / CHECKS_PENDING`
> **Executor:** `Codex`, único escritor
> **Autorizado em:** `2026-08-11T19:59:55-03:00`
> **Branch:** `task/f4.8-verification-repair-loop`
> **Baseline:** `d4e34c7404d28a10969ab4b322748d01ae5805bf`
> **Checkpoint READY:** `checkpoint/f4.8-ready` → `bb6752c1f1524b8c747cddc55e74ed7e6491e845`
> **Produto:** `8e5e11d81c685c53ba349bab4d95cdd61ee19ba6`

## Problema comprovado

A F4.7 persiste a suíte canônica e mantém uma reprovação íntegra em `VERIFYING`, mas o lifecycle ainda
não compõe essa evidência com o retry. Em `ExecutionLifecycleService`:

- `verify()` recupera a única suíte persistida e devolve novamente a reprovação, sem reexecutá-la;
- `resume()` rejeita qualquer execução em `VERIFYING` com `VerificationRequiredError`;
- toda tentativa F4.7 está fixada em `attempt: 1` e exige a suíte completa original;
- o `RetryContext` F2.6 nasce apenas de `RetryEvidence` entregue por um backend de nó, não de
  `VerificationSuiteResult`.

Baseline reproduzido no commit exato acima:

```text
.\.venv\Scripts\python.exe -m pytest tests/unit/test_verification_lifecycle.py tests/e2e/test_retry_context.py -q --basetemp build/pytest-f48-baseline
6 passed, 1 warning in 10.92s
```

O warning é somente a cache opcional do pytest sem permissão no checkout. O sucesso desses testes
confirma as duas primitivas isoladas; não demonstra o loop exigido pela F4.8. Hoje nenhuma falha F4.7
retorna ao nó corretor, nenhuma segunda tentativa de gates existe e custo/tempo não impedem retry.

## Baseline conhecido

| Item | Evidência observada |
|---|---|
| `main` local/remota | `d4e34c7404d28a10969ab4b322748d01ae5805bf`, limpa e sincronizada antes da branch |
| Fechamento anterior | F4.7 `PROMOTED / RECONCILED / CLOSED` |
| CI pós-merge anterior | run `31541047111`, evento `push`, 11/11 checks verdes no SHA de `main` |
| Branch exclusiva | `task/f4.8-verification-repair-loop`, criada do baseline e sem upstream |
| Runtime | `.venv\Scripts\python.exe`, Python `3.12.13`; `uv` indisponível no `PATH` |
| Dossiês ativos anteriores | zero |
| Tags/publicação | nenhuma tag F4.8, nenhum push e nenhum PR |

## Escopo congelado

### Contrato operacional

1. `ExecutionLifecycleService` continua dono da composição: recupera a última tentativa canônica,
   resolve a policy imutável, calcula limites e constrói o `RetryContext` a partir dos `GateResult`.
2. A origem do contexto é o nó determinístico que referencia a policy de verificação; o destino é
   exclusivamente seu `on_failure`, que deve ser um nó corretor compilado e limitado por
   `retry_policy`.
3. `GraphExecutor` continua dono da travessia: uma programação externa durável liga o terminal
   verificado ao destino validado, reutiliza o último input persistido desse nó e preserva a cadeia de
   digests, tentativas, CAS, lock e fencing.
4. `stdout`, `stderr`, IDs dos gates, commit reprovado, instrução e orçamento derivados da evidência
   são redigidos e persistidos antes do novo efeito corretivo.
5. Após a correção, somente os gates antes reprovados podem ser executados como tentativa dirigida.
   Essa tentativa nunca conclui a execução. Se passar, a suíte completa é executada e persistida no
   mesmo commit limpo; somente ela pode autorizar `COMPLETED`.
6. Todas as tentativas — completa, dirigida, reparo e bloqueio — devem ser recuperáveis após crash,
   sem duplicar subprocesso, efeito corretivo ou transição terminal.

### Limites duráveis

- por nó: `retry_policy.max_iterations`, contado pelo journal de nós existente;
- por execução: `model_routing.retry_max`, contado por programações de reparo persistidas;
- tokens: `max_tokens_per_node`, calculado dos `model_calls` persistidos após o início do repair loop;
- custo: uso real persistido de prompt/completion multiplicado por taxas contábeis explícitas da
  policy; é custo governado do retry, não claim de faturamento do provedor;
- tempo: deadline absoluto derivado da primeira programação e de
  `max_retry_duration_seconds`, persistido e reutilizado nas retomadas.

Ausência, corrupção ou divergência de policy, usage, deadline, contexto, digest, commit ou sequência
de eventos bloqueia fechado. O orçamento de `RetryContext` passa a ser projeção do ledger; valores
fornecidos apenas pelo backend não autorizam uma nova tentativa.

### Arquivos permitidos

- `src/ai_engineering_harness/runtime/execution_lifecycle.py`;
- `src/ai_engineering_harness/runtime/graph_executor.py`;
- `src/ai_engineering_harness/runtime/node_executors.py`;
- `src/ai_engineering_harness/contracts/policies.py` e exports estritamente necessários;
- `src/ai_engineering_harness/defaults/policies/retry_cost_policy.yaml`;
- componentes de `src/ai_engineering_harness/verification/` somente para seleção observável de
  requirements, sem aceitar gates fora da policy;
- testes unitários/E2E e fixtures diretamente ligados ao contrato;
- `README.md`, `TASK.md`, `docs/tasks/README.md`, este dossiê, walkthrough/guia/auditoria e testes
  documentais necessários para claims honestos.

### Proibido

- implementar promoção/cherry-pick/revert F3.7 ou marcar candidate/promotion commit;
- integrar automaticamente worktree, tools ou provider ao lifecycle padrão;
- permitir escolha manual de gate, suíte vazia, gate desconhecido ou conclusão por tentativa dirigida;
- enfraquecer limpeza/identidade Git, redaction, lock, CAS, fencing, write-ahead ou guard F4.7;
- inventar custo de provedor: as taxas contábeis devem estar explícitas na policy;
- alterar dependências, `uv.lock`, CI, versão do pacote, schemas globais ou tarefas posteriores;
- push, PR, merge, tag remota ou exclusão de refs sem autorização nominal específica.

## Critérios de aceite congelados

```yaml
composition:
  canonical_f47_result_is_the_only_retry_source: true
  responsible_node_is_compiled_verification_on_failure: true
  graph_executor_replays_only_compiled_nodes: true
retry_context:
  includes_failed_gate_ids: true
  includes_redacted_stdout_and_stderr: true
  includes_failed_commit_and_actionable_instruction: true
  remaining_budget_is_ledger_derived: true
attempts:
  every_full_targeted_and_repair_attempt_is_durable: true
  per_node_limit_blocks: true
  per_execution_limit_blocks: true
  crash_resume_is_idempotent: true
budgets:
  token_usage_comes_from_persisted_model_calls: true
  policy_accounted_cost_blocks_next_attempt: true
  absolute_retry_deadline_blocks_next_attempt: true
verification:
  targeted_retry_contains_only_previously_failed_gates: true
  targeted_success_never_completes: true
  final_full_suite_uses_same_clean_commit: true
  completed_requires_nonempty_all_required_passed_full_suite: true
e2e:
  intentionally_broken_commit_fails: true
  correction_node_receives_canonical_failure_and_repairs: true
  affected_gate_then_full_suite_pass: true
  promotion_eligible_only_after_full_suite: true
quality:
  focused_tests_green: true
  phase4_compatibility_green: true
  full_regression_green: true
  mypy_ruff_compileall_green: true
  isolated_wheel_build_and_smoke_green: true
```

O E2E usa repositório Git temporário e commit real propositalmente quebrado. O backend corretor
injetado deve observar o contexto F4.7, corrigir o arquivo e produzir um novo commit limpo; o provider
de worktree do teste atualiza explicitamente a referência para esse SHA. Isso prova a composição sem
alegar a integração automática de worktree/provider que permanece fora desta tarefa.

## Rollback

Antes de qualquer publicação, rollback é abandonar a branch ou reverter commits F4.8 de forma
não destrutiva, preservando dossiê e evidência negativa. Depois de PR, usar commits corretivos ou
`git revert` autorizado; nunca reescrever histórico compartilhado. Payloads/eventos novos devem ser
aditivos e recuperáveis; incompatibilidade observada mantém o gate bloqueado e exige recongelamento.

## Checklist de liberação

### Validação documental e evidência negativa

A primeira rodada concluiu `21 passed, 4 failed, 2 warnings, 6 subtests passed`: quatro regressões
ainda exigiam o estado transitório `ADMIN_PR_OPEN / CHECKS_PENDING` da F4.7. Depois de atualizar
somente os claims temporais, a segunda rodada concluiu `24 passed, 1 failed, 2 warnings, 6 subtests
passed`; restava no índice a expectativa literal “administrativo #48 aberto”. A correção final
registrou merge `d4e34c7` e CI `31541047111`; a terceira rodada concluiu
`25 passed, 1 warning, 6 subtests passed`. O warning é exclusivamente a cache opcional do pytest.

```text
[x] problema comprovado por execução e referências exatas
[x] baseline Git limpo, sincronizado e com CI pós-merge verde observado
[x] branch exclusiva criada do merge administrativo F4.7
[x] runtime Python >=3.11 detectado e executor único registrado
[x] escopo, contrato, limites, proibições e efeitos congelados
[x] critérios de aceite exatos e não redutíveis congelados
[x] rollback não destrutivo definido
[x] documentação e encoding validados
[x] commit documental criado
[x] checkpoint/f4.8-ready criado localmente
```

O commit documental `bb6752c` recebeu a tag local `checkpoint/f4.8-ready` antes da primeira alteração
de produto. A precedência do gate foi preservada integralmente.

## Implementação local

O commit `8e5e11d` implementa a composição sem ampliar a fronteira congelada:

- `ExecutionLifecycleService` recupera tentativas F4.7 sequenciais e usa somente a última suíte
  canônica reprovada como fonte do reparo;
- `VerificationRepairRequest` vincula tentativa/digest/SHA, policy de retry, origem/destino, deadline
  absoluto e `RetryContext`; o `GraphExecutor` valida a aresta `on_failure`, persiste o schedule e
  continua somente pelo grafo compilado;
- o contexto inclui IDs, stdout/stderr redigidos, commit quebrado, instrução e saldo calculado do
  ledger; `RetryBudget` ganhou tempo restante e o contrato de policy ganhou taxas contábeis explícitas
  de entrada/saída e duração máxima;
- depois do efeito corretivo, a tentativa dirigida contém exatamente os gates obrigatórios antes
  reprovados. Sucesso dirigido nunca conclui: a suíte integral é executada no mesmo commit limpo e
  somente ela pode transicionar para `COMPLETED`;
- limites de nó, execução, tokens, custo contábil e deadline absoluto bloqueiam duravelmente em
  `FAILED_RETRY_EXHAUSTED`; divergência de evento, payload, contexto, digest, commit ou sequência
  falha fechada;
- a recuperação aceita schedule persistido com CAS de cursor pendente e aplica o cursor antes de
  executar, sem duplicar o efeito corretivo.

O E2E usa repositório Git e commits reais, backend corretor determinístico e provider de worktree
injetados. Isso prova a composição F4.8 sem alegar integração automática dos backends no produto.

## Evidência positiva final

| Gate | Resultado observado |
|---|---|
| Baseline F4.7/F2.6 | `6 passed` |
| Contratos/policy/grafo focados | `87 passed` |
| GraphExecutor existente | `46 passed` |
| Lifecycle F4.7 existente | `4 passed` |
| F4.8 E2E final | `7 passed`, inclusive crash no CAS do cursor |
| Foco agregado | `56 passed` antes da prova adicional; todos permaneceram cobertos pela regressão final |
| Retomada E2E existente | `6 passed` |
| Unitários separados | `738 passed, 4 skipped, 6 subtests passed` |
| E2E separados antes da prova adicional | `19 passed, 1 skipped`; arquivo F4.8 final passou `7/7` |
| Regressão integral final | `758 passed, 5 skipped` em 763 itens |
| Documentação/ledger/encoding final | `25 passed, 6 subtests passed` |
| Mypy | zero issues em 106 módulos de `src`; foco com o teste novo, 107 arquivos |
| Ruff / compileall / diff check | todos exit 0 |
| Pacote | sdist e wheel `0.1.0` construídas da cópia isolada do produto `8e5e11d` |
| Smoke externo | wheel instalada em venv temporário; package/metadata/CLI `0.1.0`, exports F4.8 e zero bytecode |
| Escopo Git | oito arquivos no commit de produto; dependências, lock, CI, versão do pacote e tarefas posteriores intactos |

Os cinco skips finais são integrações live opt-in ou diferenças POSIX conhecidas: Serena live,
provider de modelo live e três provas Windows de launcher/symlink POSIX. O único warning integral é a
cache opcional do pytest sem permissão de escrita; nenhum teste foi omitido.

## Evidência negativa preservada

1. A primeira rodada de contratos produziu 36 falhas pela ausência do import `math` no novo validator;
   o import foi corrigido e o grupo passou `87/87`.
2. O lifecycle focado encontrou duas falhas de desserialização estrita de enum/data; a recuperação
   passou a validar o JSON canônico e concluiu `4/4`.
3. O E2E principal expôs primeiro cardinalidade terminal e depois cursor divergente após a transição
   de estado; o schedule recebeu revisão recuperável/CAS de cursor e o cenário passou.
4. A fixture de custo serializou `1e-06` como string YAML; a representação decimal fixa restaurou o
   contrato numérico sem relaxar a policy.
5. O mypy focado encontrou sete erros e o Ruff seis problemas de import/ordem/estilo; todos foram
   corrigidos antes das execuções integrais.
6. A primeira suíte unitária ampla obteve `736 passed`, duas falhas ambientais e quatro skips: um
   `basetemp` dentro do repo expôs deliberadamente o fixture binário inválido ao teste de encoding e
   `%LOCALAPPDATA%` colidiu com `proj-123` sem permissão. Repetida com temporários externos isolados,
   concluiu `738 passed, 4 skipped`.
7. A primeira tentativa da prova adicional terminou em sete erros de setup porque o diretório global
   `pytest-of-walla` não era legível. Com `basetemp` exclusivo, o injetor inicialmente atingiu o CAS
   normal do entrypoint; armado somente depois da reprovação canônica, comprovou o crash correto e o
   arquivo final passou `7/7`.
8. O primeiro build PEP 517 foi bloqueado no bootstrap isolado de `setuptools` e o módulo `build`
   mascarou o stderr local com `UnicodeDecodeError`. Repetido com acesso temporário autorizado,
   construiu ambos os artefatos.
9. O smoke encontrou sequencialmente ausência de `pip` na `.venv` gerenciada, bloqueio de leitura da
   wheel no sandbox, quoting de `python -c` e bootstrap `.pth` do pywin32. A instalação final ocorreu
   sem dependências/rede em venv descartável e o probe passou com origem externa e zero bytecode.
10. O primeiro gate documental final obteve `24 passed, 1 failed, 6 subtests passed`: o painel
    registrava o produto como `8e5e11d`, mas a regressão exigia identidade completa. O SHA integral foi
    adicionado ao campo da branch e a repetição concluiu `25 passed, 6 subtests passed`.

Nenhuma dessas ocorrências foi contabilizada como sucesso. Cada causa foi corrigida ou isolada e o
gate correspondente foi repetido até exit 0.

## Estado Git local

- branch: `task/f4.8-verification-repair-loop`, rastreando a branch homônima em `origin`;
- baseline: `main == origin/main == d4e34c7`;
- gate documental: `bb6752c`; produto: `8e5e11d`;
- `checkpoint/f4.8-ready` existe somente localmente e aponta para `bb6752c`;
- commit de certificação e `checkpoint/f4.8-complete` são a saída desta certificação local;
- o PR #49 está aberto; nenhum merge, tag remota, exclusão de ref ou início da F3.7 ocorreu.

## Publicação autorizada

> **Observado em:** `2026-08-11T21:37:54-03:00`
> **Autorização nominal:** `autorizo` — publicar a branch e abrir o PR F4.8
> **Estado:** `PR_OPEN / CHECKS_PENDING`

| Evidência | Resultado observado |
|---|---|
| Branch remota | `origin/task/f4.8-verification-repair-loop`, criada sem force-push e configurada como upstream |
| Tags | checkpoints READY/COMPLETE preservados somente localmente |
| Pull request | [#49](https://github.com/Wf-ops1/Harnessinfra/pull/49), base `main`, head inicial `5bf0d75e6878f0d1362e0b2053a228a95ec80cef` |
| Estado remoto | PR aberto; checks ainda não auditados; merge não autorizado |

## Checklist de conclusão local

```text
[x] checkpoint READY anterior ao produto
[x] fonte canônica F4.7 e aresta compilada comprovadas
[x] contexto redigido e budgets derivados do ledger
[x] targeted seguido de full no mesmo commit limpo
[x] limites de nó, execução, tokens, custo e tempo fail-closed
[x] crash-resume sem duplicar efeito corretivo
[x] regressão integral, mypy, Ruff, compileall e diff check verdes
[x] sdist/wheel e smoke externo do commit de produto verdes
[x] documentação pública reconciliada para conclusão local
[x] commit documental final e checkpoint/f4.8-complete criados
```

## Próxima ação exata

Auditar todos os checks do PR #49 no head final. Merge, tag remota, promoção, reconciliação e F3.7
permanecem proibidos até nova autorização explícita.
